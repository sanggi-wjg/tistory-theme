# 상쾌한기분 티스토리 스킨

`sanggi-jayg.tistory.com`의 커스텀 티스토리 스킨을 직접 제작하는 프로젝트.

**상위 규범** — 판단이 필요하면 이 둘을 먼저 본다.
- [DECISIONS.md](./DECISIONS.md) — 확정 결정과 근거, 플랫폼 제약, 실측, 미결 사항
- [DESIGN.md](./DESIGN.md) — 디자인 시스템 (토큰·타이포·컴포넌트·티스토리 고정 마크업 대응)

## 작업 방식

**파일을 바꾸는 작업은 브랜치에서 시작한다.** `main`에 직접 커밋하지 않는다.

```
1. git switch main && git pull   ← 기점을 최신 origin/main 으로 맞춘다
2. git switch -c <이름>
3. … 작업 …
4. npm run check                 ← 빌드 → 린트 → 프리뷰. 통과해야 커밋
5. 커밋
6. /pr-review-gate               ← 브랜치 diff 리뷰. 차단이 0이어야 PR이 열린다
7. 푸시 → PR 생성
8. 머지되면 정리                  ← 아래 「머지 후 정리」
```

**1번을 건너뛰지 않는다.** `git switch -c`는 **지금 있는 커밋**에서 딴다 — 낡은 `main` 위나 남의 브랜치 위에서 따면 그 위에 쌓인다.

**예외는 읽기뿐이다.** 질문에 답하거나 코드를 훑어보는 것처럼 아무것도 바꾸지 않는 작업은 그냥 한다.

브랜치 이름은 작업 범위로 짓는다 — `home-grid`, `toc-scrollspy`, `inline-fix`.

**커밋 전 필수** — `npm run check`가 통과해야 한다. 린트 오류가 남은 채로 커밋하지 않는다. 이 도메인은 조용히 실패하므로 린트가 유일한 조기 경보다.

**PR 전 필수** — `/pr-review-gate`. `npm run check`와 **보는 축이 다르다.** 린트는 규칙이
**있는가**를 보고, 리뷰는 그 규칙이 이 변경에서 **실제 조건을 재현하는가**를 본다.
아래 「이 도메인의 핵심 위험」에 적힌 세 번의 위조된 통과 신호가 전부 린트 초록불이었다.

⚠ **이 저장소에서 「훅」은 두 가지를 뜻한다.** `docs/hooks.md`는 **스킨 마크업 훅 계약**
(skin.html이 내보내는 class·id)이고, `.claude/hooks/`는 **Claude Code 훅**이다.
완전히 다른 것이고 서로 참조하지 않는다.

게이트는 Claude Code 훅으로 강제된다(`.claude/hooks/pr-review-gate.py`) — PR 생성 명령은
`.claude/.pr-review-ok`의 SHA가 HEAD와 같을 때만 통과한다. **마커를 손으로 찍지 않는다.**
찍는 순간 "이 커밋을 읽었고 문제가 없다"는 뜻이 되고, 거짓이면 게이트는 없느니만 못하다.

**작업 한 사이클은 PR 생성까지가 기본이다.** 사용자가 매번 요청하지 않아도 브랜치 → 커밋 → **리뷰** → 푸시 → PR까지 진행한다. 다만 **PR을 merge하지는 않는다** — 병합은 사용자의 판단이다. 확신이 서지 않는 변경은 `--draft`로 연다. **머지된 뒤의 정리는 다시 내 몫이다.**

**PR 본문에 담을 것**
- **무엇을** 바꿨는가 — 파일 나열이 아니라 결과로
- **왜** — `DECISIONS.md`의 결정 번호나 `DESIGN.md` 절을 참조
- **어떻게 확인했는가** — 린트 결과, 프리뷰에서 본 페이지, **검증하지 못한 것**
- 스킨 변경이면 프리뷰 스크린샷

**번호로 식별하는 목록에는 항목을 슬러그로 더한다.** `DECISIONS.md` 미결처럼 번호로 매긴 목록은 여러 작업이 동시에 붙으면 반드시 겹친다. 기존 번호는 다른 문서가 참조하므로 그대로 둔다.

### 머지 후 정리 — 요청을 기다리지 않는다

**PR이 머지된 것을 확인하면 그 자리에서 브랜치를 정리한다.** 내가 머지했든 사용자가 직접 했든 같다.

```bash
gh pr view <번호> --json state --jq .state   # MERGED 여야 한다. 이것부터 확인한다
git switch main && git pull
git branch -D <이름>       # 스쿼시 머지는 커밋을 그대로 남기지 않아 -d는 거부한다
git fetch --prune          # 원격은 머지 때 GitHub이 지운다(deleteBranchOnMerge). 죽은 추적 참조만 턴다
```

**확인하지 않은 것은 지우지 않는다.** `MERGED`가 아니면 그대로 둔다. 닫히기만 한 PR(`CLOSED`)의 브랜치에는 **아직 아무 데도 없는 작업**이 들어 있다.

## 하네스: 티스토리 스킨 제작

**목표:** 치환자·고정 마크업·수동 배포라는 티스토리 제약 위에서, 조용히 실패하지 않는 스킨을 만든다.

**트리거:** 스킨 구현·수정·기능 추가 요청 시 `tistory-skin-orchestrator` 스킬을 사용하라. 치환자 질문 같은 단순 조회는 직접 응답 가능.

**SEO는 별도 축이다.** 검색 유입·색인·내부링크·구조화 데이터 요청은 `seo-auditor` 에이전트가 맡는다. 배포 직후에는 `seo-verify-live` 스킬로 **프로덕션 실물**을 확인한다 — 소스 린트는 배포 사고를 원리적으로 잡지 못한다. 무엇이 우리 레버이고 무엇이 티스토리 소관인지는 `DECISIONS.md` 결정 28에 있다.

**글쓰기 습관은 스킨의 레버가 아니다.** 본문 첫 소제목이 글 제목과 같은 것(실측 10/10)은
확인된 습관이고 **스킨은 건드리지 않는다 — 다시 묻지 않는다**(결정 45). JS로 감춰도
검색엔진에는 그대로 보이므로 SEO 동기로 여는 것 자체가 성립하지 않는다.

**이 도메인의 핵심 위험:** 티스토리에는 컴파일러가 없다. 치환자 오타는 무시되고, CSS 선택자 불일치는 에러를 내지 않으며, JS 셀렉터는 `null`을 반환하고 끝난다. **셋 다 조용히 실패한다.** 그래서 배포 전 `skin-qa-check` 린트가 필수다.

**두 번째 위험은 결이 다르다 — 결정이 무효로 만든 문장을 같이 못 고친다.**
2026-08-27 셀프 리뷰에서 셋이 나왔다. 결정 36이 `--wrap`을 1520으로 올려 놓고
결정문에 안 적어 **정본 두 문서가 서로 다른 값**을 말했고, 결정 44는 `--error`의
사용처를 옮기며 "이제 쓰는 데가 없다"를 세 곳에 적었는데 생성기 안의 사용처를
못 봤으며, 결정 29가 뒤집은 전제를 요구하는 린트가 **주석 한 줄에 매달려** 조용히
살아 있었다. **셋 다 `npm run check`가 원리적으로 못 본다** — 린트는 "규칙이
있는가"를 보고 이건 "**전에 적은 문장이 아직 참인가**"다.
상수·토큰·치환자를 바꿨으면 **그 이름으로 저장소 전체를 훑는다.** `src`만으로는
부족하다 — 생성기(`scripts/build.mjs`)가 문자열로 쓰는 사용처는 `dist`에만 있다.

**검증 도구가 통과 신호를 위조한 적이 네 번 있다.** 프리뷰가 영역 중첩 규칙을 몰랐고, 카테고리 치환자 두 종류를 같은 마크업으로 그렸고, 티스토리 시트를 실지 않아 특이도 싸움이 재현되지 않았고, **댓글 수 치환자를 세 영역 모두 숫자로 채워 "값이 비는" 조건을 한 번도 그리지 않았다**(결정 42). 넷 다 실제 배포에서만 드러났다. **린트·프리뷰가 통과하면 "그 검사가 실제 조건을 재현하는가"부터 의심하고, 재현하지 않으면 검사부터 고친다.**

**하네스 변경 이력**

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-08-27 | 린트 `BND009` + `test:markup`(12케이스) 추가 | `skills/skin-qa-check/scripts/lint.py`, `…/test-markup-css.py`, `docs/hooks.md §7`, `package.json` | `skin.html`이 내보내는 **142종**이 어느 검사에도 안 걸렸다. `BND004`는 JS가 찾는 이름만, `BND006`은 JS가 만드는 이름만 본다 — 가장 큰 표면이 비어 있었다 |
| 2026-08-27 | `BND006`·`BND007` 파서가 **§5.6 + §8**을 읽는다 (22 → 35종). §5.1의 목차 클래스는 §5.6 표로 이동 | `…/lint.py`, `…/test-js-dom-classes.py`(12→14), `docs/hooks.md` | 결정 40이 "§5.6이 정본"이라 했는데 상태 클래스는 §8에, 목차 항목은 §5.1에 있었다. `toc.js`에서 `.toc-link`를 개명해도 **아무것도 안 켜졌다** |
| 2026-08-27 | 린트 `TOK006` 추가 (토큰 정의 ↔ 참조) | `…/lint.py`, `…/SKILL.md` | 결정 44가 `--error`를 "사용처 없다"고 세 문서에 적었는데 틀렸다. 사용처가 **생성기의 문자열 안**이라 `src` grep에 안 잡혔다. **빌드 산출물까지** 봐야 한다 |
| 2026-08-27 | `AREA003` 폐기 · `AREA005` 신설 · `lint_area_scope`가 주석을 벗긴다 | `…/lint.py`, `…/SKILL.md` | `AREA003`이 결정 29에 뒤집힌 전제를 요구하고 있었고, **뜨지 않던 이유가 `skin.html` 주석 한 줄**이었다 |
| 2026-08-27 | `npm run test:codes` 추가 — 린트 코드가 전부 `SKILL.md` 표에 있는가 | `…/test-lint-codes.py`, `package.json` | `SUB008`·`SUB009`·`TIS003`·`TIS004`가 표에 없었다. **네 번의 배포 사고에서 태어난 넷**이다. 안 적힌 축은 안 덮인 축으로 읽힌다 |
| 2026-08-27 | 프리뷰에 `protected` 페이지 추가, 본문 픽스처에 12줄 코드블록·강조색 2종 추가 | `skills/skin-preview/scripts/render.py`, `…/SKILL.md` | 보호글은 렌더러가 통째로 버려 `.protected-*` 여덟 종이 화면에 나온 적이 없었고(결정 3의 범위), 줄번호 거터와 강조색 매핑 분기도 한 번도 안 그려졌다 |
| 2026-08-26 | PR 리뷰 게이트 추가 — 스킬 + PreToolUse 훅 | `skills/pr-review-gate`, `hooks/pr-review-gate.py`, `agents/skin-qa.md`, 오케스트레이터 Phase 5 | PR 전 리뷰가 사이클에 없었다. 린트는 규칙의 존재만 봐서 "검사가 이 변경을 볼 수 있는가"를 묻는 자리가 없었다 |
| 2026-08-26 | 린트 `HLJS002` 추가 | `skills/skin-qa-check/scripts/lint.py`, `skills/skin-qa-check/SKILL.md` | 결정 44가 "구문 색은 코드 전용 토큰만 쓴다"를 규칙으로 세웠는데 주석 말고는 지키는 것이 없었다. `--link`를 다시 쓰면 조용히 통과한다 |
| 2026-08-26 | `probe-code-detect.mjs`를 `npm run check`에 넣고(`test:code`) `authorLanguage()` 표본 17개 추가, 프리뷰 본문에 펜스형 코드블록 3종 추가 | `scripts/probe-code-detect.mjs`, `skills/skin-preview/scripts/render.py`, `package.json` | 「어느 라벨을 믿는가」가 규칙이 됐는데(결정 43) 그 규칙을 보는 검사가 없었다. 프로브는 수동 실행이라 회귀를 못 잡았다 |
| 2026-08-26 | 린트 `BND008` 추가, 프리뷰가 **빈 치환자 조건**을 그리도록 수정, `npm run check`에 `test:decor` 추가 | `skills/skin-qa-check/scripts/lint.py`, `…/test-empty-decor.py`, `skills/skin-preview/scripts/render.py`, `package.json` | 값이 비었는데 CSS 장식만 남는 실패가 **두 번째**였다(태그 `,` → 댓글 라벨). 프리뷰가 세 영역 모두 숫자로 채워 그 조건을 한 번도 재현하지 않았다 (결정 42) |
| 2026-08-26 | 린트 `BND006`·`BND007` 추가, `npm run check`에 `test:lint` 단계 추가 | `skills/skin-qa-check/scripts/lint.py`, `…/test-js-dom-classes.py`, `docs/hooks.md §5.6`, `package.json` | JS가 **만드는** 클래스를 보는 검사가 없었다(`BND004`는 찾는 쪽만 본다). 새 린트가 실제로 켜지는지는 린트 자신이 증명할 수 없어 테스트를 같이 넣었다 (결정 40) |

변경 이력은 `git log`, 결정의 근거는 `DECISIONS.md`에 있다.
