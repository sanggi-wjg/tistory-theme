# HANDOFF

다음 세션이 이어받는 지점. **2026-08-25 기준 스냅샷.**

> ⚠️ **이 문서는 사용자가 요청할 때만 읽는다.** 특정 시점의 상태를 굳혀 놓은 것이라 시간이 지나면 아래 문서들과 어긋난다. 자동으로 읽으면 낡은 상태를 현재로 오인한다.
>
> **현재 상태를 알아야 하면 저장소를 직접 본다** — `git log`, `gh pr list`, `ls src/`.

이 문서는 *그 시점에 어디까지 왔고 다음에 뭘 하려 했는지*만 담는다. 결정의 내용과 근거는 [DECISIONS.md](./DECISIONS.md), 시각 규칙은 [DESIGN.md](./DESIGN.md), 작업 규칙은 [CLAUDE.md](./CLAUDE.md)에 있다. **그 셋은 살아 있는 문서이고, 이 문서는 스냅샷이다.**

---

## 지금 상태

**설계와 하네스는 끝났다. 스킨 소스(`src/`)는 아직 한 줄도 없다.**

```
main = 8250446 · 열린 PR 없음 · npm run check exit 0
```

| | |
|---|---|
| 문서 | `DECISIONS.md`(결정 27건) · `DESIGN.md`(디자인 시스템) · `CLAUDE.md`(작업 규칙) |
| 하네스 | 에이전트 5 · 스킬 7 · 스크립트 1 — 전부 실행 검증됨 |
| 데이터 | `data/` 4종. 275편 전수 실측 (카테고리는 2026-08-25 개편 적용 후 재실측) |
| 인프라 | `package.json` · `scripts/build.mjs` |
| **소스** | **없음. 여기서부터 시작한다** |

---

## 다음 작업

### 바로 시작 가능 — 스킨 구현

카테고리 개편이 끝나 **기본이미지 규칙(`DESIGN.md` §6.2)도 확정된 값으로 쓸 수 있다.** 남은 변수는 도안 자체뿐이다.

```
tistory-skin-orchestrator 스킬 실행
```

오케스트레이터가 팀 4명(마크업·스타일·동작·QA)을 꾸리고 `src/`를 만든다. Phase 0에서 `EnterWorktree`로 작업 공간을 먼저 잡으므로 사전 준비가 필요 없다.

**첫 사이클에서 확인해야 할 것들** — 아직 아무도 검증하지 못했다:

- 오케스트레이터의 **팀 실행 자체가 처음**이다. Phase 0/5의 브랜치·PR 절차가 실제로 도는지
- **인라인 스타일 보정이 브라우저에서 실제로 먹는지.** `src/`가 없어 눈으로 못 봤다. 프리뷰 `page` 페이지를 다크모드로 열어 확인한다
- **다크 팔레트가 파생값**이라 대비가 부족할 수 있다 (`DESIGN.md` §2)
- `#8a8a8a`(휘도 0.54)를 라이트 보정으로 분류했는데 경계값이다

### 끝남 — 카테고리 개편

사용자가 티스토리 관리 화면에서 수동으로 적용했다(2026-08-25). 뒤이어 `/blog-census`로 재실측하고 개편안 문서·매핑·생성 스크립트를 지웠다. **살아 있는 정본은 `DECISIONS.md` 결정 27 + §3, `DESIGN.md` §5.3·§6.2, `data/categories.json`이다.**

결과는 **상위 14 / 하위 21 · 사이드바 36줄**이고, 계획(상위 13 / 하위 19)과 몇 군데 다르다 — `PHP`가 남았고 `데이터베이스` 하위 구성이 갈렸다. 차이는 `DECISIONS.md` §7에 적혀 있다.

**남은 것**: 미결 8(`PHP` 2편 존치 여부 — 기본이미지 14장이냐 13장이냐가 여기서 갈린다) · 미결 9(비공개 3편) · 미결 6(도안 28장).

---

## 이 프로젝트에서 반드시 알아야 할 것

**티스토리에는 컴파일러가 없다. 세 가지가 조용히 실패한다.**

- 치환자 오타 → 무시되거나 문자 그대로 출력
- CSS 선택자 불일치 → 에러 없음
- JS 셀렉터 불일치 → `null` 반환하고 끝

그래서 **커밋 전 `npm run check`가 규칙**이다. 실제로 이 세션에서 세 번 물렸다:

| 무엇 | 어떻게 드러났나 |
|---|---|
| 인라인 보정 선택자에 공백이 빠져 **609곳 중 1곳만 매칭** | 린트 시나리오를 짜다 발견. 지금은 빌드가 실측 데이터에서 두 형태를 생성한다 |
| 대표이미지 "80%" | RSS 최신 50편 표본이었다. 전수는 **45.1%** |
| 인라인 폰트 "4종" | 파서가 오래된 글 33편을 놓쳤다. 실제 **13종** |

전부 `DECISIONS.md` §7에 정정 기록으로 남겼다. **수치를 인용하기 전에 집계 범위를 확인하라.**

---

## 함정 모음

| 함정 | 내용 |
|---|---|
| 본문 래퍼 | `<div class="tt_article_useless_p_margin contents_style">`. **정확일치로 찾으면 오래된 글이 누락된다** |
| 홈 vs 목록 | 홈은 `<s_index_article_rep>` + `[##_article_rep_*_##]`, 목록은 `<s_list_rep>` + `[##_list_rep_*_##]`. 섞으면 **빈 화면** |
| `<s_t3>` | 빠지면 댓글·공유가 전부 죽는다 |
| `index.xml` | 바꾸면 **스킨의 모든 설정이 초기화된다** |
| `[##_list_rep_title_##]` | New 아이콘 `<img>`가 섞인다. 순수 텍스트는 `_text_` |
| `data-ke-language` | 39%만 있고 그마저 틀리다. `javascript` 44개는 실제 JS가 0개 |
| 배포 | **비공식 API를 쓰지 않는다.** 사용자가 스킨 편집기에 직접 붙여넣는다 → 업로드 파일은 `images/script.js` 1개뿐이어야 한다 |
| 동시 작업 | 두 세션이 같은 디렉터리에서 `git add -A`를 돌려 **커밋이 뒤섞인 적이 있다.** worktree로 분리한다 (`CLAUDE.md`) |

---

## 명령

```bash
npm run check      # 빌드 → 린트 → 프리뷰. 커밋 전 필수
npm run build      # dist/ 생성
npm run preview    # _preview/ 8개 페이지 → open _preview/index.html
npm run lint       # 정적 린트

python3 .claude/skills/blog-census/scripts/census.py --posts --bodies   # 전수 재실측
```

---

## 남은 미결 (`DECISIONS.md` §4)

| # | 항목 | 막고 있는 것 |
|---|---|---|
| 1 | 스킨 편집기 **파일 개수·용량 제한** 실측 | 배포 |
| 4 | 모바일웹 OFF의 광고 수익 영향 | 최종 전환 |
| 5 | 다크 팔레트 파생값 검증 | — (구현 중 확인) |
| 6 | 기본이미지 도안 28장 | 미결 8의 판단 |
| 7 | 스킨 미리보기 이미지 4종 | 배포 |
| 8 | `PHP` 2편 존치 여부 | 사용자 판단 |
| 9 | 비공개 3편 배치 | 사용자 판단 |
| 10 | `알고리즘` 이름 | — (그대로 두기로 함) |

**1·4·5·7·10은 스킨 구현을 막지 않는다.**
