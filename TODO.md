# TODO — 본 블로그 적용 후 개선 목록

`sanggi-jayg.tistory.com` 라이브에서 확인된 것과 사용자가 지적한 것을 모은다.
**확정 결정은 여기가 아니라 [DECISIONS.md](./DECISIONS.md)에 남긴다.**
여기는 "아직 안 한 일"만 있는 목록이고, 끝나면 줄을 지우고 결정/설계 문서로 옮긴다.

각 항목은 **실측 근거**를 달고 들어온다. 이 도메인은 조용히 실패하므로
"그런 것 같다"로 항목을 만들면 고쳐도 고쳐졌는지 알 수 없다.

번호는 다른 문서가 참조할 수 있으므로 재사용하지 않는다. 항목을 더할 때는
번호가 아니라 **슬러그**로 식별한다(CLAUDE.md 「번호로 식별하는 목록」).

> 2026-08-26에 이 파일은 한 번 비어서 지워졌다(`f2645ab`). 그때까지의 닫힌 항목과
> 어느 결정으로 갔는지는 `git show f2645ab^:TODO.md`에 그대로 있다.

---

## `codeblock-readability` ② — 전수에서 몇 %가 칠해지는지 모른다

**①·③은 닫혔다** (결정 44 · 결정 43). 남은 것은 이 측정 하나다.

`detect()`는 신뢰도 미달이면 **아무것도 하지 않고 원문으로 둔다**(`code.js`의 `detect()`) —
한국어 메모가 엉뚱한 언어로 물드는 것을 막으려는 의도된 설계다. 문제는 **그 대가를
전수로 재본 적이 없다**는 것이다.

- 임계는 `code.js`의 `MIN_RELEVANCE`(4) · `MAX_HANGUL`(0.25) · `MIN_CHARS`(12)
- `scripts/probe-code-detect.mjs`는 **픽스처로 임계를 검증**할 뿐, 실제 728개에
  대한 커버리지 집계가 아니다

**표본 하나는 나쁘지 않았다.** 2026-08-26 라이브 `타이밍 어택…CVE까지` 글에서
`<pre>` 10개 중 **10개가 `code-wrap`으로 감싸졌고 8개가 칠해졌다(80%)** —
감지 언어는 Python 4 · Kotlin 3 · Java 1. 못 칠한 2개는 세 줄짜리 짧은 블록이다
(임계 설계대로 동작한 것이다). **한 편은 전수가 아니다.**

**답하는 질문은 하나뿐이다** — 임계가 너무 빡빡한가? 낮게 나오면 임계를 손볼
근거가 되고, 높게 나오면 그대로 둔다. **어느 쪽이든 지금 화면이 나빠지고 있다는
뜻은 아니다** — 못 칠한 블록은 원문 그대로 남고, 결정 43 덕분에 글쓴이가 펜스
하나로 개별 블록을 고칠 수 있다. 급하지 않다.

**재는 법이 이미 있고, `census-pre-class`와 같은 자리다.** `census.py`가 본문을
받아 `<pre>`를 뽑고 있으므로 거기서 `detect()`를 돌리면 된다. **둘을 한 번에 한다**
— 크롤링을 두 번 돌릴 이유가 없다.

### 하이라이팅 말고 가독성에 걸리는 것 (같이 보되 섞지 않는다)

- 코드 폰트 13px / 줄높이 1.7 (`.contents_style pre` · DESIGN.md §6.3) — 재보지 않았다
- **줄바꿈을 하지 않는다.** 최대 1,777자짜리 줄이 있어 `overflow-x: auto`로 정한
  것이고(§6.3), 긴 줄은 가로로 밀린다. 바꾸려면 그 결정을 다시 재야 한다
- 줄번호는 8줄 이상에서만 켜진다 (`code.js`의 `LINES_FOR_NUMBERS`)

---

# 라이브 실물 검토 (2026-08-26)

`sanggi-jayg.tistory.com` 홈과 글 페이지를 브라우저로 열어 본 것에서 나온 항목이다.
**전부 화면과 `getComputedStyle`/DOM 실측이고, 재현 경로를 달았다.**
검토 조건 — 1440×1000 · 라이트/다크 양쪽 · 홈 1페이지(카드 13개) ·
글 `타이밍 어택…CVE까지` + 홈 카드 12편 HTML 대조.
**못 본 것: 모바일 폭**(창 리사이즈가 실제 뷰포트에 반영되지 않아 재지 못했다) ·
**댓글/방명록** · 카테고리·검색·태그·보관함 페이지.

---

## `census-pre-class` — 실측 도구가 `<pre class>`를 세지 않는다

`census.py:103`은 `data-ke-language="([^"]*)"`만 센다. 그래서 §3의 코드블록 통계
(728개 중 285개 라벨, 39%)에 **에디터가 `<pre>` 클래스로 박아 둔 라벨이 통째로
빠져 있다.**

`fence-markup-probe`에서 우연히 드러났다 — 홈 12편의 `<pre>` 41개 중
`data-ke-language`는 5개인데 **`<pre class>`는 38개**였다(최빈값 `reasonml` 6).
즉 지금 통계는 라벨 있는 블록을 **크게 과소 집계**하고 있다.

**고칠 것** — `census.py`가 `<pre>`의 `class`와 `<code>`의 `class="language-*"`를
같이 세게 한다. 셋이 서로 다른 신호이므로 **합치지 말고 따로 센다**:

| 신호 | 누가 만들었나 | 믿을 수 있나 |
|---|---|---|
| `data-ke-language` | 에디터 | ❌ (결정 18) |
| `<pre class>` | 에디터 자동 감지 | ❌ (최빈값이 `reasonml`) |
| `<code class="language-*">` | **글쓴이** | ✅ (단 왕복하면 사라진다) |

**수치가 바뀌면 `DECISIONS.md` §3도 같이 고친다.** 728·285·39%가 그 문서에 있다.

---

## `excerpt-spacing` — 카드 발췌에서 문장이 붙는다

**`title-echo`에서 갈라져 나온 항목이다.** 그 뿌리(첫 소제목이 제목의 복제)는
결정 45로 닫혔지만, **이건 그것과 무관하게 남는 별개의 결함이다.**

`[##_list_rep_summary_##]`가 본문을 앞에서 자를 때 **블록 요소 경계에 공백을
넣지 않는다.** 그래서 문장이 붙어 나온다 (2026-08-26 홈 카드 실측, **13개 중 7개**):

```
…프레임워크 CVE까지아래는 어디서나 볼 법한…
…살펴보자.1. 타이밍 어택…
```

코드블록 텍스트도 그대로 섞여 흐른다 (`if request_value != secret_value: raise …`).

**마크업으로는 못 고친다.** 발췌는 티스토리가 **만들어서 주는 문자열**이라
치환자를 바꿔서 될 일이 아니다. JS 후처리만 가능하다.

**정하지 못한 것** — 고칠 것인가. 고친다면 `.post-excerpt`의 텍스트를 JS가 다시
쓰는 것인데, 그 자리는 **20개 카드마다 도는 자리**라 비용을 재야 한다.
그리고 어디에 공백을 넣을지 알 수 없다 — 원문의 블록 경계 정보가 이미 사라진
뒤의 문자열이라 `.`·`다`·숫자 앞 같은 **휴리스틱이 된다.** 휴리스틱으로 본문을
고치는 것은 이 저장소가 피해 온 방식이다(결정 18·43).

**재현 경로** — 홈(`/`) 카드 발췌 첫 줄. `norm(excerpt)`에 `[가-힣][A-Z0-9]` 또는
`[.다요][^\s]` 패턴이 있는지 센다.

---

## `doc-line-refs` — 문서의 `파일:줄번호` 인용이 조용히 썩는다

2026-08-26 한 세션에서 **낡은 인용 5건**이 나왔다. `src/js/code.js` 위쪽에 60줄이
들어가면서 아래가 전부 밀렸는데, 그것을 가리키던 문서는 그대로였다.

| 인용 | 가리키던 곳 | 실제로 가리키게 된 곳 |
|---|---|---|
| `code.js:104~106` | `detect()`의 조기 반환 | 상수 블록 |
| `code.js:85`·`45`·`44` | 임계 상수 셋 | 엉뚱한 줄 |
| `code.js:46` | `LINES_FOR_NUMBERS` | 다른 상수 |
| `code.js:29` (2곳) | `LANGS` | 다른 줄 |

**그중 둘은 PR #39에서 새로 쓴 것이었다** — 쓸 때 이미 틀렸는데 리뷰가 놓쳤다.
`npm run check`는 이런 걸 보지 않는다.

**이번에는 다섯 곳을 `code.js`의 `LANGS`처럼 심볼 인용으로 바꿨다.** 심볼은
줄이 밀려도 안 썩는다.

> **2026-08-27 정정 — "나머지 9곳은 지금 전부 맞다"고 적었는데 하나가 틀렸다.**
> `docs/hooks.md`의 `skin.html:283`이 `<s_rp>`를 가리키고 있었다(실제는 291행).
> 손으로 대조한 것이 놓쳤다. 심볼 인용(`skin.html`의 `.toc-toggle`)으로 바꿨다.
>
> 같은 날 저장소 전체의 `파일:줄` 인용 **20건을 기계로 대조**했고 틀린 것은
> 그 하나뿐이었다. 즉 항목의 전제(인용이 썩는다)는 유효하고, **"손으로 대조하면
> 된다"는 대응만 성립하지 않는다.**

**정하지 못한 것 — 검사를 만들 것인가.**
- 심볼 인용(`` `파일`의 `심볼` ``)은 **검사가 가능하다** — 그 문자열이 파일에
  있는지 grep하면 된다. 줄번호 인용은 파일 길이만 볼 수 있어 밀림을 못 잡는다
- 그러려면 인용 형식을 규범으로 정해야 하고, 기존 문서를 일괄로 바꿔야 한다
- **가치가 비용을 넘는지 아직 모른다.** 5건이 한 세션에서 나왔다는 것만 사실이다
- 2026-08-27 데이터 한 점 — 전체 **20건 중 1건**이 썩어 있었고, 그것을 찾는 데 쓴
  스크립트는 30줄이었다(줄 존재·범위 초과만 본다). 심볼 인용까지 보려면 인용 형식을
  규범으로 정해야 한다. **여전히 미결이다 — 사용자 판단**

---

## `lightbox-origin` — 라이트박스가 원본을 띄우는지 모른다

`lightbox.js`는 오랫동안 이렇게 쓰여 있었다:

```js
// 원본 URL이 따로 있으면 그걸 쓴다 (티스토리 이미지 블록은 축소본을 넣는다)
big.src = img.getAttribute('data-origin') || img.currentSrc || img.src
```

**`data-origin`의 근거가 저장소 어디에도 없다** (2026-08-27 셀프 리뷰). 실제로
관찰된 티스토리 이미지블록은 다른 이름을 쓴다 — 프리뷰 픽스처가 실물을 보고
만든 것인데 거기에는 `data-origin-width`·`data-origin-height`와
`<span data-url="…">`만 있다.

**없는 속성을 먼저 보는 코드는 폴백 덕에 조용히 통과한다.** 화면은 멀쩡하고
"원본을 쓰고 있다"는 착각만 남는다. 그래서 그 줄을 지웠다 — 지금은
`currentSrc || src`뿐이다. **화면은 이전과 같다**(어차피 그 속성은 없었으니까).

**정하지 못한 것 — 티스토리가 원본 주소를 어디에 두는가.**
- 후보 ① `<span data-url>` (이미지블록이 `<img>`를 감싼다). 픽스처에서는 `src`와
  같은 값이라 **이것이 원본인지 갈리지 않는다**
- 후보 ② `src`의 CDN 파라미터 (`kage@…` 계열의 리사이즈 접미사)
- **재현 경로** — 큰 원본(2000px 이상)을 하나 올린 글에서 서버 원본 HTML을 받아
  `<img src>`·`data-url`·주변 속성을 나란히 보고, 라이트박스를 열어 실제 픽셀 폭을
  잰다. 셋이 같으면 지금이 최선이고, 다르면 그때 그 이름을 붙인다
- 급하지 않다. 라이트박스는 뷰포트 크기로 띄우므로 **지금도 화면에서는 커진다.**
  잃는 것은 확대했을 때의 선명도뿐이고, 그마저 아직 잃고 있는지 확인되지 않았다

---

## 닫힌 항목

| 슬러그 | 무엇 | 어디로 갔나 |
|---|---|---|
| `list-comment-count` | 목록에서 댓글 수가 비었는데 `댓글`·`·`만 남았다 (홈 13/13 · 사이드바 5/5) | PR #34 · **결정 42** · 린트 `BND008` |
| `fence-markup-probe` | 티스토리가 마크다운 펜스를 무엇으로 저장하는가 | **측정 완료.** 라벨 신뢰 규칙과 왕복 소실은 **결정 43**으로 갔다 |
| `codeblock-readability` ③ | 언어를 글쓴이가 직접 지정 | PR #37 · **결정 43** · 검사 `test:code`(표본 17개) |
| `codeblock-readability` ① | 코드 구문 색이 사실상 3색이었다 (문자열·숫자가 기본색보다 흐리고 함수명은 동일) | PR #39 · **결정 44** · 린트 `HLJS002` · `DESIGN.md §8 → §8.1` |
| `content-callouts` | 인용문 안의 `[!NOTE]` 표식을 강조 박스로 | **하지 않기로 했다 (2026-08-27, 사용자).** 착수 전이라 코드는 없다. 흩어져 있던 참조(토큰 주석·`code.js`·프리뷰 픽스처·결정 43·44)를 걷어냈다 |
| `title-echo` | 본문 첫 소제목이 글 제목의 복제 (홈 12편 중 10편) | **결정 45** — 글쓰기 습관이고 스킨은 건드리지 않는다. 발췌 공백 문제만 `excerpt-spacing`으로 갈라 남겼다 |

---

## 규칙 — 항목을 더할 때

1. **재현 경로를 쓴다.** 어느 페이지, 어느 테마, 어느 폭에서 보이는지.
2. **실측을 붙인다.** 색은 대비값, 크기는 px, 특이도는 어디서부터 이기는지.
3. **원인을 추정으로 쓰지 않는다.** 못 밝혔으면 "원인 미상"이라고 쓴다.
4. 끝나면 항목을 지우고, 배울 게 있으면 `DECISIONS.md`로 옮긴다.
