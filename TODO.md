# TODO — 본 블로그 적용 후 개선 목록

`sanggi-jayg.tistory.com` 라이브에서 확인된 것과 사용자가 지적한 것을 모은다.
**확정 결정은 여기가 아니라 [DECISIONS.md](./DECISIONS.md)에 남긴다.**
여기는 "아직 안 한 일"만 있는 목록이고, 끝나면 줄을 지우고 결정/설계 문서로 옮긴다.

각 항목은 **실측 근거**를 달고 들어온다. 이 도메인은 조용히 실패하므로
"그런 것 같다"로 항목을 만들면 고쳐도 고쳐졌는지 알 수 없다.

번호는 다른 문서가 참조할 수 있으므로 재사용하지 않는다. 항목을 더할 때는
번호가 아니라 **슬러그**로 식별한다(CLAUDE.md 「번호로 식별하는 목록」).

> 2026-08-26에 이 파일은 한 번 비어서 지워졌다(`f2645ab`). 그때까지의 닫힌 항목과
> 어느 결정으로 갔는지는 `git show f2645ab^:TODO.md`에 그대로 있다.

---

## 요청 — 마크다운 펜스를 쓰고 싶다 (2026-08-26, 사용자)

> "확장자별 codeblock과 다른 마크다운 기반 문서에서 볼 수 있는
> ` ```info `, ` ```warning `, ` ```error ` 등을 사용 가능하도록 하고 싶다."
>
> 보충 (같은 날) — "확장자별 코드블록은 **코드 하이라이팅 등을 추가해서 가독성을
> 높이고 싶다**는 내용이었다."

**목적은 가독성이지 문법이 아니다.** 언어 지정은 수단이고, 지금 코드블록이 읽기
어려운 진짜 이유는 따로 있을 수 있다 — 아래 `codeblock-readability`가 그것부터 잰다.

아래 세 항목으로 쪼갠다. `fence-markup-probe`는 `content-callouts`의 선행조건이고,
`codeblock-readability`의 세 번째 레버(언어 직접 지정)의 선행조건이기도 하다.
**다만 `codeblock-readability`의 앞 두 레버는 그 측정 없이 지금 착수할 수 있다.**

---

## `fence-markup-probe` — 티스토리가 펜스를 **무엇으로 저장하는가** (측정)

**콜아웃과 「언어 직접 지정」은 이걸 재기 전에 구현하지 않는다**
(하이라이팅 색·커버리지는 이것과 무관하게 진행할 수 있다).
우리는 티스토리가 발행 시점에 만들어
놓은 HTML만 받는다. 에디터에서 무엇을 치든, 남는 마크업이 우리가 붙잡을 수 있는
유일한 훅이다. 지금 **모르는 것**을 그대로 적는다:

- 에디터 마크다운 모드에서 ` ```python `은 `<pre data-ke-type="codeblock" data-ke-language="python">`이 되는가, 아니면 언어가 버려지는가
- ` ```info `처럼 **티스토리가 모르는 이름**을 넣으면 → ① `data-ke-language="info"`로 그대로 남는가 ② 빈 값이 되는가 ③ 코드블록 자체가 안 되는가
- 발행 후 **다시 편집**해도 그 속성이 살아남는가 (에디터 왕복에서 지워지면 규칙 전체가 무너진다)
- HTML 모드로 직접 `data-ke-language`를 박아 넣으면 저장 때 살아남는가

**재현 경로** — 테스트 글 한 편에 6종 펜스(`python` · `kotlin` · `javascript` ·
`info` · `warning` · 언어 없음)를 넣고 발행 → 본문 HTML을 받아 `<pre>` 6개의
속성을 그대로 옮겨 적는다. 그다음 편집 화면을 한 번 열었다 저장하고 다시 받는다.
수집기는 이미 있다 — `.claude/skills/blog-census/scripts/census.py:103`이
`data-ke-language="([^"]*)"`를 세고 있다.

**결과가 ③이면 요청의 절반은 여기서 끝난다** — 펜스 이름이 마크업에 남지 않으면
JS가 볼 것이 없다. 그때는 콜아웃을 인용문(`<blockquote data-ke-style>`)이나
접두 문자열 같은 **다른 저작 관례**로 다시 설계해야 한다.

---

## `codeblock-readability` — 코드블록을 읽기 쉽게 (하이라이팅)

**하이라이팅은 이미 붙어 있다.** `src/js/code.js`가 highlight.js로 자동 감지해
칠하고, 언어 라벨·복사 버튼·조건부 줄번호까지 있다(결정 9·18 · DESIGN.md §6.3).
그런데도 읽기 어렵다면 원인은 셋 중 하나이고, **셋은 각각 다른 레버다.**
어느 것이 실제 원인인지는 **아직 안 쟀다** — 먼저 라이브 글 몇 편을 눈으로 보고
갈라야 한다(추천: 파이썬 글 하나, 셸·설정 섞인 글 하나, 긴 로그 덤프 하나 · 라이트/다크 양쪽).

### ① 색이 거의 없다 — **가장 유력하고, 이미 문서에 적혀 있다**

DESIGN.md **§8 알려진 빈틈**이 그대로 옮겨 온다: "**코드블록 728개가 사실상 무채색으로
보인다.**" §6.3이 언어 라벨과 신뢰도만 정하고 `.hljs-*` **색을 정하지 않아서**,
`components.css:1412~1451`이 기존 범용 토큰만으로 임시 팔레트를 짜 두었다.
실제로 쓰이는 색은 **네 개뿐**이다:

| `.hljs-*` | 지금 색 | |
|---|---|---|
| 주석·인용 | `--ink-mute` | |
| 키워드·타입·내장·리터럴 | `--link` | 유일하게 유채색 |
| 함수명·섹션 | `--ink` (600) | |
| **문자열·숫자·변수·속성·정규식** | `--ink-body` | **본문 잉크와 같은 색** |

**하이라이팅에서 가장 먼저 눈에 들어와야 할 축(문자열·숫자)이 본문과 같은
회색이다.** 제대로 하려면 §8이 적어 둔 대로 **코드 전용 색 토큰 4~5종**
(문자열·숫자·주석·키워드)을 새로 만들고 라이트/다크 **세 면 전부**에서 대비를 재야 한다.
범용 토큰을 빌려 쓰는 대가는 2026-08-26에 이미 한 번 청구됐다(`--link`·`--error`가
라이트에서 AA 미달이었고 **코드블록 배경 위에서만** 드러났다 — §8.1 표). **이 항목의 진짜
값어치는 색이 예뻐지는 것이 아니라 그 연결이 끊기는 것이다.**

**2026-08-26 라이브에서 직접 쟀다. ①이 맞았다.** `타이밍 어택…CVE까지` 글에서
`getComputedStyle`로 받은 실제 색이다 — 이론이 아니라 화면에 칠해진 값이다:

| `.hljs-*` | 라이트 | 다크 | |
|---|---|---|---|
| (코드 기본색) | `#171717` | `#ededed` | 기준 |
| 키워드·타입·내장·리터럴 | `#0064da` | `#3291ff` | **유일한 유채색** |
| 주석 | `#707070` | `#8f8f8f` | |
| **문자열·숫자** | `#4d4d4d` | `#b0b0b0` | **기본색보다 흐리다** |
| **함수명·params** | `#171717` | `#ededed` | **기본색과 완전히 같다 — 구분 0** |

**색이 네 개가 아니라 사실상 셋이고, 그중 하나는 후퇴색이다.** 하이라이팅에서
가장 먼저 눈에 들어와야 할 문자열·숫자가 기본 코드색보다 **흐리게** 칠해진다 —
강조가 아니라 반대로 간다. 함수명은 아예 기본색이라 `<span>`을 붙이고도 화면에
차이가 없다. **두 테마 모두 같은 구조다.**

같은 글에서 색이 붙은 문자 비율은 **43.3%**(8,188자 중 3,545자, span 246개)였다.
칠하는 양이 부족한 게 아니라 **칠한 색이 서로 안 갈린다**는 뜻이다.

### ② 칠해지지 않는 블록이 얼마나 되는지 모른다

`detect()`는 신뢰도 미달이면 **아무것도 하지 않고 원문으로 둔다**(`code.js:104~106`) —
한국어 메모가 엉뚱한 언어로 물드는 것을 막으려는 의도된 설계다. 문제는 **그 대가를
전수로 재본 적이 없다**는 것이다.

- 임계는 `MIN_RELEVANCE = 4`, 한글 비중 가드 25%, 최소 12자 (`code.js:85`·`45`·`44`)
- `scripts/probe-code-detect.mjs`는 **픽스처로 임계를 검증**할 뿐, 실제 728개에
  대한 커버리지 집계가 아니다
- 즉 **"728개 중 몇 개가 실제로 칠해지는가"는 미측정이다**

**표본 하나는 나쁘지 않았다.** 2026-08-26 라이브 `타이밍 어택…CVE까지` 글에서
`<pre>` 10개 중 **10개가 `code-wrap`으로 감싸졌고 8개가 칠해졌다(80%)** —
감지 언어는 Python 4 · Kotlin 3 · Java 1. 못 칠한 2개는 세 줄짜리 짧은 블록이다
(`if request_value != secret_value:` 등 — 임계 설계대로 동작한 것이다).
**한 편은 전수가 아니다.** 이 숫자가 275편에서도 유지되는지는 여전히 미측정이고,
그래도 **①이 ②보다 먼저다** — 80%를 칠해도 색이 안 갈리면 소용이 없다.

**재는 법이 이미 있다.** `census.py`가 본문을 받아 `<pre>`를 뽑고 있으므로
(`.claude/skills/blog-census/scripts/census.py:101`), 거기서 `detect()`를 돌려
칠해진 비율과 감지된 언어 분포를 집계하면 된다. **커버리지가 낮다면 ③이 답이고,
높다면 ①이 답이다** — 이 숫자가 다음 작업을 정한다.

### ③ 언어를 글쓴이가 직접 지정한다 (` ```python `)

②의 커버리지가 낮을 때 쓰는 레버다. **지금은 라벨을 일부러 무시하고 있다** —
`code.js`는 `data-ke-language`를 버린다(결정 18). 근거는 실측이었다:

| | 실측 (2026-08-25 전수) |
|---|---|
| 코드블록 | 728개 |
| `data-ke-language`가 있는 것 | 285개 (**39%**) |
| 그중 `javascript` | 44개 — **전부 오답** (실제로는 셸·설정·SQL·한국어 메모) |
| 한국어가 섞인 블록 | 239개 (**33%**) |

**그래서 "라벨이 있으면 라벨을 쓴다"로 뒤집으면 과거 글이 망가진다.** 최소한 44개가
확실히 틀린 라벨을 다시 신뢰하게 된다. 요청은 **앞으로 쓰는 글**에 대한 것이므로
과거와 미래의 규칙이 갈리는 지점이고, **결정 18을 고쳐 써야 하는 사안이다.**

정하지 못한 것:

- **어떻게 가르는가.** 날짜로 자를 수 없다(마크업에 발행일이 없다). 후보 —
  ① 알려진 오답 라벨(`javascript` 등)만 계속 무시하고 나머지는 신뢰 ② 라벨과
  자동 감지가 **어긋날 때만** 감지를 따른다 ③ 새 글에만 붙는 별도 표식을 쓴다
- **언어 목록을 넓혀야 한다.** 지금 번들에 든 것은 10종뿐이다
  (`python bash shell sql java kotlin go json yaml xml` — `code.js:29`).
  라벨을 신뢰하기로 하면 `javascript`·`typescript`·`css`·`html`·`dockerfile` 같은
  **번들에 없는 이름**이 들어오고, 그때 무엇을 할지가 규칙에 없다(현재 코드는
  `LABEL[res.language]`에 없으면 조용히 원문으로 둔다 — `code.js:104`).
- **번들 예산.** 현재 `dist/images/script.js`는 **63,648 B**(highlight.js core + 10종 포함).
  언어를 늘리면 여기서 는다. 결정 20이 Font Awesome을 뺀 이유가 용량이었으므로
  같은 저울에 올려야 한다.
- **저작 수단이 있는지부터 확인한다** — `fence-markup-probe`. 펜스 이름이 마크업에
  남지 않으면 이 레버 자체가 없다.

**감지 쪽 안전망은 어느 경우에도 그대로 둔다.** 임계 4와 한글 25% 가드는 라벨이
**없는** 443개를 위해 계속 필요하다.

### 하이라이팅 말고 가독성에 걸리는 것 (같이 보되 섞지 않는다)

- 코드 폰트 13px / 줄높이 1.7 (`.contents_style pre` · DESIGN.md §6.3) — 재보지 않았다
- **줄바꿈을 하지 않는다.** 최대 1,777자짜리 줄이 있어 `overflow-x: auto`로 정한
  것이고(§6.3), 긴 줄은 가로로 밀린다. 바꾸려면 그 결정을 다시 재야 한다
- 줄번호는 8줄 이상에서만 켜진다 (`code.js:46`)

---

## `content-callouts` — ` ```info ` · ` ```warning ` · ` ```error `를 강조 박스로

**색부터 막힌다. 이건 이미 측정된 값이다** (DESIGN.md **§8 알려진 빈틈**):

| 토큰 | 값 | 라이트 대비 | 판정 |
|---|---|---|---|
| `--error` | `#d60000` | 5.44 / 5.21 / 4.99 (**세 면 전부**) | 본문 글자로 **쓸 수 있다** |
| `--warning` | `#f5a623` | **1.94:1** (`#fafafa` = `--canvas-soft`) | 본문 글자로 **못 쓴다** |
| `--accent-cyan` | `#29bc9b` | **2.30:1** (`#fafafa` = `--canvas-soft`) | 본문 글자로 **못 쓴다** |

다크에서는 셋 다 통과한다(각각 6.74~5.93 · 11.3 · 12.4). **흰 캔버스(`--canvas`
`#ffffff`)에서는 저 둘이 더 나빠진다** — DESIGN.md §8의 수치는 한 단 어두운
`--canvas-soft` 기준이다. **즉 콜아웃을 만들면
라이트에서만 무너지는 컴포넌트가 된다.** 선택지는 둘 — ① 색을 테두리·배경·아이콘에만
쓰고 글자는 `--ink`로 둔다 ② 라이트 전용으로 한 단 어둡게 파생한 값을 토큰에 넣는다
(§8의 조정안). **②를 고르면 `--warning`·`--accent-cyan`의 첫 텍스트 사용처가 되므로
그 미결이 여기서 청구된다.**

`--error`에는 별도의 함정이 있다. `tokens.css:47`이 "유일한 사용처가 `.hljs-deletion`이라
**항상** `--canvas-soft` 위에 놓인다"고 적어 두었다 — 콜아웃이 두 번째 사용처가 되면
그 주석의 전제가 깨진다. **값을 바꾸지 않더라도 주석은 같이 고쳐야 한다.**

그 밖에 정하지 못한 것:

- **이름을 몇 개나 받을 것인가.** `info`·`warning`·`error`에 `note`·`tip`·`success`를
  더할지. 늘릴수록 색 토큰과 아이콘이 같이 는다.
- **접근성.** 색만으로 종류를 가르면 안 된다 — 아이콘이나 접두 라벨이 필요하고,
  스크린리더에 무엇으로 읽힐지(`role`·시각적 라벨)를 정해야 한다.
- **`<pre>`를 벗겨낼 것인가.** 콜아웃은 코드가 아니므로 등폭 폰트·가로 스크롤·
  줄번호·복사 버튼이 전부 어울리지 않는다. `initCode`가 `<pre>`를 전부 훑으므로
  (`code.js:234`) **콜아웃을 먼저 걸러내지 않으면 복사 버튼이 달린다.**
- **검사가 이걸 볼 수 있는가.** 새 클래스를 JS가 만들면 `BND006`·`BND007`이
  대응 CSS 규칙을 요구한다(결정 40). 프리뷰(`skin-preview`)의 mock 본문에도
  콜아웃 표본이 있어야 화면으로 확인할 수 있다 — **없으면 또 통과 신호가 위조된다.**

---

# 라이브 실물 검토 (2026-08-26)

`sanggi-jayg.tistory.com` 홈과 글 페이지를 브라우저로 열어 본 것에서 나온 항목이다.
**전부 화면과 `getComputedStyle`/DOM 실측이고, 재현 경로를 달았다.**
검토 조건 — 1440×1000 · 라이트/다크 양쪽 · 홈 1페이지(카드 13개) ·
글 `타이밍 어택…CVE까지` + 홈 카드 12편 HTML 대조.
**못 본 것: 모바일 폭**(창 리사이즈가 실제 뷰포트에 반영되지 않아 재지 못했다) ·
**댓글/방명록** · 카테고리·검색·태그·보관함 페이지.

---

## `list-comment-count` — 목록에서 댓글 수가 비었는데 **라벨과 구분자만 남는다**

**결정 35의 `tag-comma`와 정확히 같은 실패다** — 값이 비었는데 구분자를 CSS가
무조건 그린다. 그때는 태그의 `,`였고 이번엔 댓글 라벨이다.

| 자리 | 마크업 | CSS | 실측 (2026-08-26 라이브) |
|---|---|---|---|
| 홈 카드 | `skin.html:161` `<span class="post-rp">[##_list_rep_rp_cnt_##]</span>` | `components.css:277` `.post-rp::before { content: "댓글 "; }` | **카드 13개 전부 비었다 (13/13)** → 날짜 뒤에 `댓글`만 뜬다 |
| 사이드바 최근 글 | `skin.html:393`·`416` `<span class="side-rp">[##_rctps_rep_rp_cnt_##]</span>` | `components.css:1153` `.side-rp::before { content: "· "; }` | **5개 전부 비었다 (5/5)** → 날짜 뒤에 `·`만 뜬다 |
| 글 페이지 | `skin.html:207` `<s_rp_count>…댓글 [##_article_rep_rp_cnt_##]</s_rp_count>` | — (라벨이 마크업에 있다) | **정상** — `댓글 0`으로 나온다 |

**재현 경로** — 홈(`/`)을 열고 카드의 날짜 오른쪽을 본다. 라이트/다크 무관.
DOM으로는 `document.querySelectorAll('.post-rp')`가 전부 `textContent === ''`다.
사이드바 쪽은 티스토리가 `<span id="commentCountOnRecentEntries292"></span>`라는
**빈 span만** 내놓는다.

**원인은 미상이다.** 세 치환자 모두 `data/substitutions.json` 화이트리스트에 있고
린트는 통과한다. 목록용 두 개가 왜 빈 문자열을 내는지 — 댓글이 0이라 그런지,
목록 영역에서 원래 안 채워지는지 — **재보지 않았다.** 댓글이 있는 글을 만들어
확인하는 것이 가장 싸다.

**축 3의 사례이기도 하다.** 린트는 치환자 **이름이 유효한가**만 보고
**실제로 값을 내는가**는 못 본다. 그래서 이 결함은 라이브에서만 보인다.

**고칠 때의 선택지** — ① `<s_rp_count>` 같은 조건 블록이 목록 영역에도 있는지
확인해 그것으로 감싼다 ② 라벨·구분자를 CSS `::before`에서 빼고 마크업으로 옮긴다
③ JS가 빈 `.post-rp`/`.side-rp`를 지운다(가장 약한 수단 — JS가 죽으면 되돌아온다).
**②가 `tag-comma` 때 쓴 방향과 같다.**

---

## `title-echo` — 본문 첫 소제목이 글 제목의 복제다 (증상 셋, 뿌리 하나)

홈 카드 12편의 HTML을 받아 페이지 `<h1>`과 본문 첫 소제목을 글자 단위로 대조했다.

| | 실측 (2026-08-26, 홈 1페이지 12편) |
|---|---|
| 본문 첫 `h2`가 `<h1>`과 **완전히 같다** | **12편 중 10편** |
| 나머지 2편 | 본문에 소제목이 **아예 없는** 잡담 글(`맛있는 치킨스톡`·`출근길…`) |
| → 소제목이 있는 글만 보면 | **10편 중 10편 (100%)** |

**이건 스킨 버그가 아니라 글쓰기 습관이다.** 그런데 스킨의 세 자리에서 증상으로 나온다:

1. **글 페이지에서 제목이 두 번 보인다** — `<h1>` 바로 아래 본문 첫 줄이 같은 문장이다
2. **목차 첫 항목이 제목이다** — `util.js:83`의 `headingsWithIds()`가
   `h2, h3`를 그대로 긁고 `toc.js`가 그것을 쓰므로 목차가 "제목 → 1. → 2. …"로
   시작한다. 실측: 목차 첫 항목 = `<h1>`과 동일 문자열. **`heading-anchor.js`도
   같은 목록을 쓰므로**(hooks.md §5.8) 복제 제목에 앵커 알약까지 붙는다
3. **홈 카드 발췌가 제목으로 시작한다** — `skin.html:158`의
   `[##_list_rep_summary_##]`가 본문 텍스트를 앞에서 자르는데 그 앞이 복제 제목이다.
   **카드 13개 중 11편(85%)의 발췌가 자기 제목으로 시작한다**

**발췌에는 별개의 문제가 하나 더 붙어 있다.** 블록 요소 경계에 공백이 없어 문장이
붙는다 — `…프레임워크 CVE까지아래는 어디서나 볼 법한…`, `…살펴보자.1. 타이밍 어택…`.
**13개 중 7개**에서 관찰됐다. 코드블록 텍스트도 그대로 섞여 흐른다
(`if request_value != secret_value: raise ...`). 발췌는 티스토리가
`[##_list_rep_summary_##]`로 **만들어서 주는 문자열**이라 마크업으로는 못 고치고
JS 후처리만 가능하다.

**재현 경로** — 홈(`/`) 카드 발췌 첫 줄, 글 페이지 제목 아래 첫 줄, 그리고 목차 첫 항목.
대조는 `norm(h1.textContent) === norm(contents_style의 첫 h2.textContent)`.

**정하지 못한 것 — 어디서 고칠 것인가.**

- **글에서 고친다** (275편의 첫 소제목을 지운다) — 뿌리를 없애지만 **콘텐츠 루프**이고
  275편 수작업이다. 미결 13이 같은 이유로 열려 있다
- **스킨에서 감춘다** — 본문 첫 소제목이 `<h1>`과 같으면 JS가 감추고 목차에서도 뺀다.
  **글을 안 건드리고 세 증상을 한 번에 없앤다.** 다만 *글쓴이가 의도한 소제목*을
  스킨이 지우는 것이라, 제목을 나중에 고치면 조용히 되살아난다
- **발췌만 고친다** — 카드 발췌에서 선행 제목만 떼고 블록 경계에 공백을 넣는다.
  ③만 풀고 ①②는 남는다

**어느 쪽이든 `docs/hooks.md`와 SEO를 같이 봐야 한다.** 본문 첫 `h2`를 감추면
헤딩 계층이 바뀌므로 `seo-auditor` 축이다. 그리고 **JS로 감추면 검색엔진에는
계속 보인다** — 색인상의 중복은 안 없어진다.

---

## 규칙 — 항목을 더할 때

1. **재현 경로를 쓴다.** 어느 페이지, 어느 테마, 어느 폭에서 보이는지.
2. **실측을 붙인다.** 색은 대비값, 크기는 px, 특이도는 어디서부터 이기는지.
3. **원인을 추정으로 쓰지 않는다.** 못 밝혔으면 "원인 미상"이라고 쓴다.
4. 끝나면 항목을 지우고, 배울 게 있으면 `DECISIONS.md`로 옮긴다.
