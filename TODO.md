# TODO — 본 블로그 적용 후 개선 목록

`sanggi-jayg.tistory.com` 첫 배포(2026-08-26, PR #22) 이후 라이브에서 확인된 것과
사용자가 지적한 것을 모은다. **확정 결정은 여기가 아니라 [DECISIONS.md](./DECISIONS.md)에 남긴다.**
여기는 "아직 안 한 일"만 있는 목록이고, 끝나면 줄을 지우고 결정/설계 문서로 옮긴다.

각 항목은 **실측 근거**를 달고 들어온다. 이 도메인은 조용히 실패하므로
"그런 것 같다"로 항목을 만들면 고쳐도 고쳐졌는지 알 수 없다.

번호는 다른 문서가 참조할 수 있으므로 재사용하지 않는다. 항목을 더할 때는
번호가 아니라 **슬러그**로 식별한다(CLAUDE.md 「번호로 식별하는 목록」).

---

## 진행 중 / 대기

### T3 · `vercel-docs-alignment` — Vercel **docs** 기준으로 디자인 정렬

DESIGN.md는 getdesign의 Vercel **마케팅 사이트** 분석이 기반이다. 블로그 글 페이지가
실제로 닮아야 하는 건 마케팅 페이지가 아니라 **문서 페이지**다. 2026-08-26
`vercel.com/docs/functions`·`/functions/runtimes/node-js`를 다크로 실측해 대조했다.

**먼저 확인한 것 — 토큰은 이미 맞다. 숫자를 다시 베낄 필요 없다.**

| 토큰 | 우리 (다크) | Vercel docs (다크) | 판정 |
|---|---|---|---|
| 캔버스 | `#0a0a0a` | `--ds-background-100` = `hsl(0 0% 4%)` = `#0a0a0a` | 일치 |
| 본문 잉크 | `--ink #ededed` | `--ds-gray-1000` = `hsl(0 0% 93%)` = `#ededed` | 일치 |
| 실선 | `--hairline #2e2e2e` | 코드블록 테두리 `rgb(46,46,46)` | 일치 |
| 링크 | `#3291ff` | `--ds-blue-900` = `hsl(210 100% 66%)` | 일치 |
| 본문 폭 | 800px | 768px | 사실상 일치 |
| 인라인 코드 | 14.08px / `#1a1a1a` / 1px `#2e2e2e` / r4 / 1px 5px | 14.4px / `#1a1a1a` / 1px / r4 / 1px 5px | 일치 |

**그래서 남는 건 숫자가 아니라 구조와 상태 표현이다.** 아래 넷을 후보로 둔다.

- [ ] **T3-a `heading-weight`** — 본문 소제목이 실제로는 `font-weight: 400`이다.
      `content.css`가 h2·h3·h4에 600을 주는데 **라이브에서 400으로 나온다.**
      티스토리 `content.css`의 `#tt-body-page .contents_style h2`(1,1,1)가 우리
      `.contents_style h2`(0,1,1)를 이긴다 — 결정 32와 **같은 유형의 패배**다.
      실측(2026-08-26): `#tt-body-page .contents_style h2[data-ke-size]`(1,2,1)는 이긴다.
      `#tt-body-page .contents_style h2`(1,1,1)는 **진다** — 특이도가 같아 순서로 가고,
      순서는 티스토리가 정한다. Vercel docs h2는 600이고 DESIGN.md도 600이다.
      **이건 디자인 변경이 아니라 우리 설계가 반영되지 않고 있는 버그다.**
      ⚠ 고치면 글 275편의 소제목이 전부 굵어진다. 눈으로 한 번 보고 결정한다.

- [ ] **T3-b `rail-active-pill`** — 좌측 레일 활성 항목 표현.
      Vercel docs 내비: 활성 항목에 **알약 배경** `rgb(31,31,31)` + `radius 6px` +
      `padding 0 4px 0 10px`, 글자는 14px/20px w600 ls -0.28px.
      우리는 색만 바꾼다(`.tt_category li.selected > a`). 배경 알약이 훨씬 잘 읽힌다.
      `#1f1f1f`은 우리 사다리에 없다 — `--canvas-soft-2 #1a1a1a`로 갈지 토큰을
      하나 더 둘지 정해야 한다.

- [ ] **T3-c `heading-anchor`** — 소제목 앵커 링크.
      Vercel docs h2는 자식으로 `SPAN, A, A`를 갖는다 — 호버 시 `#` permalink가 뜬다.
      우리 목차는 이미 `toc-h-N` id를 만들고 있으므로 **링크만 얹으면 된다.**
      목차 없이 소제목을 공유할 수단이 지금은 없다.

- [ ] **T3-d `entry-title-scale`** — 글 제목 위계.
      우리 32px/700, Vercel docs h1 56px/600 ls -0.06em. 다만 DESIGN.md §3은
      **한글에 음수 자간을 아끼라**고 정해 뒀고(최대 -0.028em) 스케일 상한도 48px이다.
      Vercel 숫자를 그대로 가져오면 그 결정과 부딪힌다. 48px로 한 단만 올릴지,
      32px을 유지할지 — **DESIGN.md를 고칠 각오가 있을 때만** 손댄다.

**T3 착수 전 정할 것:** 넷 중 무엇을 할지. T3-a는 버그라 우선순위가 높고,
T3-b·T3-c는 순수 개선, T3-d는 상위 규범(DESIGN.md §3)과 충돌하므로 별도 판단이 필요하다.

---

## 이번 사이클에서 처리 (PR: `comment-dark-and-tag-comma`)

### T1 · `tag-comma` — 글 하단 태그의 `,` 노출 ✅

`[##_tag_label_rep_##]`는 `<a>` 사이에 **`", "` 텍스트 노드**를 끼워 넣는다.
2026-08-26 라이브 실측:

```html
<div class="entry-tags"><a href="/tag/Memory" rel="tag">Memory</a>, <a …>performance</a>, …</div>
```

`.entry-tags`가 `display:flex`라 이 텍스트 노드가 **익명 flex 아이템**이 되어
칸을 하나씩 차지한다. 그래서 태그 8개가 두 줄로 밀렸다(실측 높이 77px → 42.5px).

텍스트 노드는 선택자로 잡을 수 없다. 부모에서 `font-size:0`으로 죽이고 `<a>`에서
되살린다. **`gap`은 같이 못 쓴다** — 0폭 익명 아이템에도 gap이 붙어 태그 사이가
두 배로 벌어진다. 간격은 `margin`으로 준다.

### T2 · `comment-dark` — 다크에서 댓글이 안 보인다 ✅

**증상보다 원인이 크다.** `tistory.css`의 댓글 블록 전체가 **한 줄도 적용되지 않고 있었다.**

댓글은 `<div data-tistory-react-app="Comment">`에 **React가 런타임에 그린다.**
정적 HTML에는 빈 div뿐이라 소스만 봐서는 알 수 없고, 티스토리 시트가 우리
`style.css`보다 **뒤에** 온다(head 실측: 우리 `style.css` 0·1번, 티스토리
`content.css` 3번 · `index.css` 4번 · `tistory.css` 12번).

2026-08-26 라이브 특이도 실측 — 우리 규칙을 head 맨 앞에 넣고(= 실제 우리 위치)
어디서부터 이기는지 쟀다:

| 선택자 | 특이도 | 결과 |
|---|---|---|
| `.tt_desc` | (0,1,0) | **진다** |
| `.comments .tt_desc` | (0,2,0) | **진다** — 상대도 (0,2,0)이고 뒤에 온다 |
| `.comments .tt-box-content .tt_desc` | (0,3,0) | 이긴다 |

즉 상대는 **(0,2,0)**. 우리 댓글 블록은 전부 (0,1,0)이라 **하나도 안 먹었다.**
다크에서 실제로 나오던 색:

| 요소 | 다크 실측 | 배경 `#0a0a0a` 대비 |
|---|---|---|
| `p.tt_desc` (댓글 본문) | `#222222` | **1.16:1 — 안 보인다** |
| `a.tt-link-user` (작성자) | `#222222` | **1.16:1 — 안 보인다** |
| `div.tt-cmt` (입력 중인 글자) | `#222222` | **1.16:1 — 쓰는 글자가 안 보인다** |
| `.tt-inner-g` (입력 테두리) | `#d8d8d8` | 밝은 테두리가 다크에 뜬다 |
| `.tt-list-reply-comment` (대댓글 선) | `#f0f0f0` | 〃 |
| `.tt-box-total` (아래 구분선) | `#e9e9e9` | 〃 |

`.tt-box-total` 글자색과 입력 `<input>`은 우리 것이 이기고 있었다(요소 선택자라
상대와 안 부딪혔다). **한 블록 안에서 절반만 먹고 있던 것**이 발견을 늦췄다.

방명록도 같은 `tt-*` 클래스를 쓴다(`section.guestbook`, `#tt-body-guestbook` 실측 확인).

### T0 · `harness-reproduces-reality` — 검사가 실제 조건을 재현하게 ✅

T1·T2 둘 다 **린트도 프리뷰도 통과한 상태로 배포됐다.** CLAUDE.md 「검사가 통과 신호를
위조한 적이 세 번 있다」의 네 번째·다섯 번째다. 증상만 고치면 다음에 또 당한다.

- **프리뷰가 쉼표를 안 그렸다.** `render.py`가 `tag_label_rep`을 **공백**으로 이어
  붙였다. 티스토리는 `", "`로 잇는다. 구분자를 실물과 맞추고 태그 수도 8개로 늘렸다
  (실제 글에서 두 줄로 넘치던 개수).
- **린트에 댓글 축이 없었다.** `TIS001/TIS002`는 `content.css`(크롤 가능, `#tt-body-page`
  스코프)만 본다. 댓글은 **React가 런타임에 얹는 시트**라 원리적으로 크롤에 안 잡히고
  스코프 모양도 다르다. `data/tistory-hardcoded-colors.json`에 `commentRules` 10종을
  더하고 **TIS003**을 새로 넣었다 — 각 항목이 `.comments`·`.guestbook` **양쪽** 짝으로
  (0,3,0) 이상에 덮여 있는지 본다. 한쪽만 있으면 그것도 잡는다.
  규칙 하나를 (0,1,0)으로 되돌려 **실제로 실패하는지 확인했다.**

⚠ **여전히 못 보는 것:** 댓글 UI는 프리뷰에 나오지 않는다(React 런타임). 프리뷰는
그 자리에 `[댓글: 티스토리 React가 렌더링]`을 그려 **못 본다는 사실을 표시**한다.
댓글 변경은 배포 후 라이브에서만 최종 확인된다.

---

## 규칙 — 항목을 더할 때

1. **재현 경로를 쓴다.** 어느 페이지, 어느 테마, 어느 폭에서 보이는지.
2. **실측을 붙인다.** 색은 대비값, 크기는 px, 특이도는 어디서부터 이기는지.
3. **원인을 추정으로 쓰지 않는다.** 못 밝혔으면 "원인 미상"이라고 쓴다.
4. 끝나면 항목을 지우고, 배울 게 있으면 `DECISIONS.md`로 옮긴다.
