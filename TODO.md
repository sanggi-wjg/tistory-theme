# TODO — 본 블로그 적용 후 개선 목록

`sanggi-jayg.tistory.com` 라이브에서 확인된 것과 사용자가 지적한 것을 모은다.
**확정 결정은 여기가 아니라 [DECISIONS.md](./DECISIONS.md)에 남긴다.**
여기는 "아직 안 한 일"만 있는 목록이고, 끝나면 줄을 지우고 결정/설계 문서로 옮긴다.

각 항목은 **실측 근거**를 달고 들어온다. 이 도메인은 조용히 실패하므로
"그런 것 같다"로 항목을 만들면 고쳐도 고쳐졌는지 알 수 없다.

번호는 다른 문서가 참조할 수 있으므로 재사용하지 않는다. 항목을 더할 때는
번호가 아니라 **슬러그**로 식별한다(CLAUDE.md 「번호로 식별하는 목록」).

---

## T3 · `vercel-docs-alignment` — Vercel **docs** 기준으로 디자인 정렬

DESIGN.md는 getdesign의 Vercel **마케팅 사이트** 분석이 기반이다. 블로그 글 페이지가
실제로 닮아야 하는 건 마케팅 페이지가 아니라 **문서 페이지**다. 2026-08-26
`vercel.com/docs/functions`·`/functions/runtimes/node-js`를 다크로 실측해 대조했다.

**먼저 확인한 것 — 토큰은 이미 맞다. 숫자를 다시 베낄 필요 없다.**

| 토큰 | 우리 (다크) | Vercel docs (다크) | 판정 |
|---|---|---|---|
| 캔버스 | `#0a0a0a` | `--ds-background-100` = `hsl(0 0% 4%)` = `#0a0a0a` | 일치 |
| 본문 잉크 | `--ink #ededed` | `--ds-gray-1000` = `hsl(0 0% 93%)` = `#ededed` | 일치 |
| 실선 | `--hairline #2e2e2e` | 코드블록 테두리 `rgb(46,46,46)` | 일치 |
| 링크 | `#3291ff` | `--ds-blue-900` = `hsl(210 100% 66%)` | 일치 |
| 본문 폭 | 800px | 768px | 사실상 일치 |
| 인라인 코드 | 14.08px / `#1a1a1a` / 1px `#2e2e2e` / r4 / 1px 5px | 14.4px / `#1a1a1a` / 1px / r4 / 1px 5px | 일치 |

**그래서 남는 건 숫자가 아니라 구조와 상태 표현이다.** 하나 남았다
(`heading-weight`·`docs-gutter`·`rail-active-pill`·`heading-anchor`는 닫혔다 — 맨 아래 표).

- [ ] **T3-d `entry-title-scale`** — 글 제목 위계.
      우리 32px/700, Vercel docs h1 56px/600 ls -0.06em. 다만 DESIGN.md §3은
      **한글에 음수 자간을 아끼라**고 정해 뒀고(최대 -0.028em) 스케일 상한도 48px이다.
      Vercel 숫자를 그대로 가져오면 그 결정과 부딪힌다. 48px로 한 단만 올릴지,
      32px을 유지할지 — **DESIGN.md를 고칠 각오가 있을 때만** 손댄다.

**T3 남은 것:** T3-d 하나다. 상위 규범(DESIGN.md §3)과 충돌하므로
**DESIGN.md를 고칠 각오가 있을 때만** 연다 — 사용자 판단이 필요하다.

---

## 규칙 — 항목을 더할 때

1. **재현 경로를 쓴다.** 어느 페이지, 어느 테마, 어느 폭에서 보이는지.
2. **실측을 붙인다.** 색은 대비값, 크기는 px, 특이도는 어디서부터 이기는지.
3. **원인을 추정으로 쓰지 않는다.** 못 밝혔으면 "원인 미상"이라고 쓴다.
4. 끝나면 항목을 지우고, 배울 게 있으면 `DECISIONS.md`로 옮긴다.

---

## 닫힌 항목

| 슬러그 | 무엇 | 어디로 갔나 |
|---|---|---|
| `tag-comma` | 글 하단 태그에 `,`가 노출됐다 | PR #26 · 결정 35 끝절 |
| `comment-dark` | 다크에서 댓글이 안 보였다 (원인은 색이 아니라 **특이도**) | PR #26 · **결정 35** |
| `harness-reproduces-reality` | 위 둘을 린트·프리뷰가 통과시켰다 — `TIS003` 신설, 프리뷰 태그 구분자 수정 | PR #26 · 결정 35 |
| `docs-gutter` (T3의 새 항목) | 거터가 32px 고정이라 넓은 화면에서 답답했다 — 유동 거터(24~96px)로 | PR #28 · **결정 36** |
| `content-width-793` | 위를 재다 발견. 스크롤바 15px을 안 세서 본문이 800이 아니라 **793**이었다 | PR #28 · 결정 36 |
| `heading-weight` (T3-a) | 소제목 굵기가 **글마다 갈렸다** — `[data-ke-size]` 있는 60%만 400 | PR #28 · **결정 36-b**, 린트 `TIS004` |
| `rail-active-pill` (T3-b) | 레일 현재 항목이 왼쪽 2px 막대였다 — 알약 배경으로. 막대를 고른 근거 두 줄이 재보니 둘 다 틀렸다 | **결정 37** |
| `heading-anchor` (T3-c) | 소제목 3개 미만인 글 **32%**에는 소제목을 가리킬 수단이 없었다 | **결정 38**, hooks.md §5.8 |

처음 셋은 2026-08-26 배포에서 사용자가 확인했다.
나머지는 프리뷰 실측까지 끝났고 **라이브 확인은 배포 후**다.
