# DECISIONS.md — 상쾌한기분 티스토리 스킨

**대상** `sanggi-jayg.tistory.com` (개발 블로그, 275편)
**목표** 기존 hELLO 스킨을 버리고 커스텀 스킨을 직접 제작
**작성** 2026-08-24 그릴링 세션
**디자인 시스템** [DESIGN.md](./DESIGN.md)

이 문서는 **무엇을 왜 정했는지**의 단일 출처다. 색·타이포·간격 같은 시각 규칙은 DESIGN.md를 본다.

---

## 1. 확정 결정

### 범위

| # | 결정 | 근거 |
|---|---|---|
| 1 | 본인 블로그 전용 1개 스킨, 완전 신규 | 배포용이 아니므로 `index.xml` 변수를 최소화하고 값을 직접 박아도 된다 |
| 2 | 반응형 단일 스킨 → 티스토리 **모바일웹 OFF** 전환 | 모바일웹 자동 연결이 켜져 있던 동안 모바일 방문자는 `/m/`에서 티스토리 기본 React 스킨을 봤다 — `h1` 0개 · 다른 글로 가는 링크 0개 · 커스텀 CSS 미적용(2026-08-25 실측). 공식 문서상 **모바일 전용 스킨 슬롯은 존재하지 않는다.** 본 블로그는 2026-08-26 배포 시점에 이미 OFF였다(`0f8ca65`) |
| 3 | 전 페이지 타입 동작 보장, 디자인 투자는 홈·목록·글에 집중 | `skin.html` 한 장이 모든 URL을 담당하므로 영역 치환자를 빠뜨리면 그 URL이 빈 화면이 된다 |
| 4 | 홈은 `<s_index>` 코드 고정(커버 미사용), 단 `<cover>` 전환 가능한 블록 구조로 설계 | 커버를 쓰면 홈 구성 권한이 관리 화면으로 넘어간다. 나중에 필요하면 블록을 `<s_cover>`로 감싸기만 하면 된다 |

### 레이아웃 (결정 30)

| 페이지 | 구성 (1025px~) |
|---|---|
| 홈 | 좌측 레일 + (주목 글 1 + 3열 카드 그리드) |
| 목록 (카테고리·검색·태그·보관함) | 좌측 레일 + 목록 |
| 글 | 좌측 레일 + 본문 + 우측 목차 (1400px~ 3단) |

**근거** 두 요구가 서로 다른 방향으로 당긴다 — 긴 기술 글(코드블록 56%, 소제목 3개 이상 68%, 본문 중앙값 2,813자)은 넓은 본문 폭을, 카테고리 35종(상위 14·하위 21)은 사이드바를 원한다. `body_id`로 CSS를 분기하면 페이지 종류별로 다른 답을 줄 수 있다.

상세와 폭 실측은 결정 30·36.

### 대표이미지 없는 글 처리

| # | 결정 |
|---|---|
| 5 | 상위 카테고리별 **기본 이미지**, `data-cat` 접두 선택자로 매칭, **JS 0줄**. **2026-08-27 개정(결정 46) — WebP 래스터 (상위 14 + 기본값 1) × (light·dark) = 30장을 CSS `background-image`로 깐다.** 래스터는 마스크와 달리 색을 토큰에서 받지 못하므로 두 벌이고, 다크는 빌드가 `--ph-*`를 `tokens.css`의 3블록 패턴(`:root` 정의 → 시스템 다크·명시 다크 재정의)으로 바꿔 끼운다. 개정 전에는 SVG 15장을 CSS 마스크로 쓰고 색을 `--ink-mute`에서 받아 한 파일로 두 테마를 냈다 — 그 대가가 단색이었고, 사용자가 AI 생성 이미지로 바꾸기로 하면서 마스크를 버렸다. **지금 30장은 승인된 삽화(결 3, `src/assets/placeholders-src/*.svg`)이고 2026-08-27 본 블로그에 배포됐다** — 라이브에서 `image/webp` 200과 `style.css`의 `--ph-*` 반영을 확인했다. AI 생성은 선택이다(`docs/placeholder-image-brief.md`). 옛 모티프(`src/assets/motifs/`)는 폴백(결정 46)이라 남는다 |
| 6 | 기본 이미지는 **스킨 편집기 파일업로드 탭으로 `images/`에 올린다** — `dist/images/ph-<slug>-<theme>.v<N>.webp` 30장, 800×500(16:10), 장당 100KB 이하, 파일명에 버전(`package.json`의 `placeholderVersion` — 티스토리 CDN이 같은 이름을 오래 캐시한다). **2026-08-27 개정(결정 46).** 개정 전에는 "배포가 수동이므로 업로드 파일을 1개로" 묶으려고 SVG를 `data:` URI로 `style.css`에 인라인했고, 그 제약이 래스터를 막고 있었다 — 막은 것은 플랫폼이 아니라 우리 선택이었다(공식 문서가 `images/`에 image 업로드를 명시한다, §2). 이미지는 **바뀐 배포에서만** 올리므로 평소 배포는 여전히 붙여넣기 3 + `script.js` 1이다. 외부 호스팅(GitHub + jsDelivr)도 CSS에서 막히지 않지만 같은 도메인·공식 지원·외부 의존 0인 파일업로드를 택했다. 파일 개수 상한(미결 1)에 막히면 `scripts/build.mjs`의 `PLACEHOLDER_BASE` 하나만 CDN URL로 바꾼다 |
| 7 | **카테고리 목록 페이지에서만** 상단 배너 1장(`<s_list_image>`) + 카드는 텍스트 |
| 46 | **기본 이미지를 SVG 마스크에서 AI 생성 래스터로 바꾼다 — 결정 5·6 개정 (2026-08-27).** 사용자 요청. 검토 결과 (1) 티스토리 스킨 편집기 **파일업로드 탭으로 `images/`에 이미지를 올리는 것은 공식 지원**이고(`docs/tistory-skin-reference.txt:59-60`, 2026-08-25 실측 `script.js`가 `images/`로 갔다) (2) 외부 URL(GitHub + jsDelivr)도 CSS에서 막히지 않으며 외부 리소스 금지 결정도 없다(폰트·hljs가 CDN). 막고 있던 것은 결정 6 구판의 "업로드 파일 1개" — 플랫폼이 아니라 우리 선택이었다. 호스팅은 **① 티스토리 파일업로드**(같은 도메인·외부 의존 0·기존 배포 절차와 동일), 다크는 **두 벌 30장**, 결정 7은 **유지**, 실제 이미지는 뒤에 — 넷 다 사용자 선택. `og:image`는 여전히 못 바꾸고(§3) 홈에서 보이는 것은 2장 남짓이라는 기대치를 알고 진행했다. 구현: `scripts/prep-placeholders.mjs`(원본 → 800×500 WebP), 빌드가 `--ph-*`를 3블록으로, 린트 `TOK007`(변수는 있는데 파일이 없다). **WebP가 안 오면 옛 SVG 모티프가 다중 배경 폴백으로 드러난다**(`--ph-*-svg` data: 인라인 약 8KB, 테마 공통 회색) — 사용자 요청. 폴백은 방문자용 안전망이지 검사를 대신하지 않는다. **삽화 방향은 결 3 · 개념 일러스트(Clockwise 결을 스킨 토큰으로)로 확정**(2026-08-27, 레퍼런스 4결 중 사용자 선택 — 색은 토큰, 사람 없음, 카테고리별 피사체). 규칙·주제 사전·GPT-image 프롬프트는 `docs/placeholder-image-brief.md`. 승인된 목업 SVG 30장이 `placeholders-src/`에 들어가 지금의 기본 이미지다 |

**근거** `<s_list_rep_thumbnail>`은 대표이미지가 없으면 **블록째 사라진다.** 이를 이용해 카드 배경에 카테고리 그림을 깔고 이미지가 있을 때만 `img`가 덮게 하면 JS 없이 fallback이 된다.
결정 7이 필요한 이유: 카테고리 목록은 전부 같은 카테고리라 같은 그림이 최대 15번 반복된다 (코드 품질/Clean Code 16편 중 15편, 데이터베이스/MySQL 23편 중 15편, 코드 품질/리팩토링 9편 전부). 홈·검색·태그는 카테고리가 섞여 있어 문제가 없다. 래스터로 바꾸면서도 유지했다(2026-08-27 사용자 확인) — 그림이 좋아져도 15번 반복은 반복이다.

**기대치** — 기본 이미지는 CSS 배경이라 **`og:image`를 바꾸지 못한다**(§3 실측). AI 이미지는 사이트 안에서만 보이고, 홈은 13편 노출에 보유율 85%라 약 2장이 뜬다. 주로 보이는 곳은 검색·태그·보관함 목록이다. 이걸 알고 진행하기로 했다.

### 기능

| # | 기능 | 근거 |
|---|---|---|
| 8 | 목차(TOC) + 스크롤스파이 | 소제목 3개 이상 68%, 최대 25개 |
| 9 | 코드 하이라이팅 + 복사 버튼 + 언어 라벨 + 줄번호 | 코드블록 728개 |
| 10 | 다크모드 토글 (시스템 따름 + `localStorage`) | |
| 11 | 이미지 라이트박스 | 이미지 78%, 최대 19장, 대부분 터미널·대시보드 스크린샷 |
| 12 | 읽기 진행바 + 맨 위로 | 본문 최대 30,314자 |
| 13 | 표 가로스크롤 래핑 + 외부링크 표시 | 표 최대 4열(모바일에서 깨짐), 외부링크 65개 |
| 38 | 소제목 앵커(퍼머링크) — **목차와 무관하게** 붙인다 | 목차는 소제목 3개 이상일 때만 생긴다. 32%의 글에는 소제목을 가리킬 수단이 아예 없었다 |

**치환자만 꽂으면 되는 것** (직접 만들 필요 없음): 관련글 `<s_article_related>` · 이전/다음 글 `<s_article_prev>` `<s_article_next>` · 태그 `<s_tag_label>` · 사이드바 8종 · 댓글 · 페이징
**티스토리가 자동으로 넣는 것**: OG 태그 · 트위터카드 · JSON-LD 구조화 데이터 · `loading="lazy"` · 애드센스 본문 광고

### 기술 스택

| # | 결정 | 근거 |
|---|---|---|
| 14 | **esbuild + 순수 CSS(변수 토큰)** | 이 프로젝트는 CSS 작업의 큰 덩어리가 **내가 클래스를 붙일 수 없는 HTML**(본문·카테고리·댓글)을 다스리는 일이다. Tailwind의 강점이 닿지 않고, 쓰더라도 본문 CSS를 따로 또 써야 한다. `skin.html` 가독성도 지킨다 |
| 15 | **비공식 API 사용 안 함** — 빌드 산출물을 스킨 편집기에 직접 업로드 | 사용자 결정 |
| 16 | 프리뷰는 **로컬 mock 렌더러** | API를 안 쓰므로 치환자를 로컬에서 치환해야 한다. 275편 전수 데이터가 있어 실제 데이터로 픽스처를 만들 수 있다 |
| 17 | 댓글은 `[##_comment_group_##]` 한 줄 + `tt-*` CSS 오버라이드 | `<s_rp>` 계열 치환자는 구형이라 직접 구성하면 **핀 고정·프로필 레이어·더보기를 잃는다.** 되살릴 치환자가 없다 |
| 18 | 코드 하이라이팅은 **highlight.js 자동 감지**, `data-ke-language` 무시, 신뢰도 미달 시 하이라이트 안 함 | 728개 중 라벨이 있는 것은 285개(39%)뿐이고, `javascript` 44개는 실제로 전부 셸·설정·SQL·한국어 메모다. Prism은 자동 감지가 없고 Shiki는 빌드타임 전제라 부적합 |
| 19 | 인라인 스타일 오염은 **빌드 생성 CSS + JS 안전망 하이브리드**. 규칙은 손으로 쓰지 않고 `data/inline-styles.json`에서 생성한다 | 색 17종 609곳, 배경 11종 106곳. 종류가 적어 열거가 가능하고, 새 글은 JS가 막는다. 상세는 DESIGN.md §5.2 |
| 20 | 폰트 = Pretendard dynamic subset + JetBrains Mono + D2Coding 로컬 폴백, **Font Awesome 제거** | Font Awesome은 CSS 102KB + 웹폰트 258KB. 코드블록 33%에 한국어가 섞여 라틴 전용 코드폰트만으로는 부족하다 |
| 21 | 홈·목록 광고는 `[##_revenue_list_upper_##]` / `_lower_##]` 치환자만 심고 노출은 티스토리 관리에서 결정 | 지금은 글 페이지에만 광고가 있다. 자리를 미리 잡아두면 나중에 스킨 수정 없이 켤 수 있다 |

### 개발·배포

| # | 결정 | 근거 |
|---|---|---|
| 22 | **개발·검증 대상 블로그를 섞지 않는다.** 테스트 블로그(`git-rich-quick.tistory.com`)는 첫 배포(2026-08-26)까지 썼고, 지금 대상은 본 블로그 하나다 | 275편이 서비스 중인 블로그를 스킨이 서기 전에 건드리지 않으려던 것이다. 역할이 끝난 뒤에도 두 블로그의 지표는 섞지 않는다 — `verify.py`의 `V014`가 다른 블로그의 기준선 덮어쓰기를 거부한다 |
| 25 | 이전이 필요한 것: **GA4 `G-PHQ5FKMZ37`**, **네이버 사이트 인증 `100d6799251e62ceaf64af174e2d502e98f5f804`** — 이전 완료, `skin.html` head에 있다 | 둘 다 구 스킨 HTML 안에 있었다. 애드센스·애드핏·OG·JSON-LD·구글 서치콘솔은 티스토리가 처리하므로 이전 불필요 |
| 26 | 하네스는 **표준 범위** — 전문 에이전트 + 스킬 세트 | |
| 47 | **버전은 날짜다 (2026-08-27).** git 주석 태그 `vYYYY.MM.DD`(같은 날 재배포는 `.2`) 하나가 정본이고, 릴리즈는 배포·`seo-verify-live` 통과 **뒤**에 찍어 "이 커밋이 지금 라이브다"를 표시한다. `index.xml <version>`·`package.json version`은 건드리지 않는다. 산출물 `skin-<태그>.zip`은 CI(`.github/workflows/release.yml`)가 태그에서 `npm run check`를 통과시킨 `dist/` 전체를 붙인다 — 붙여넣기·롤백용이며 티스토리 zip 업로드는 여전히 안 된다 | 사용자 결정. 소비자 없는 개인 스킨이라 semver의 호환성 의미가 없고 배포 단위가 "그날 붙여넣은 것"이다. `index.xml`은 바뀌면 스킨 설정이 초기화돼(§2) 릴리즈마다 올릴 수 없고, `package.json`은 semver만 받아 `2026.08.27`의 앞자리 0을 거부한다 — 버전을 세 곳에 두면 서로 어긋난다. 수동 배포라 "main의 커밋"과 "라이브에 있는 것"이 다를 수 있어 표식이 필요했고, `dist/`·`backup/`이 저장소 밖이라 롤백 복사본이 없었다. 보호 규칙이 태그를 보지 않으므로 워크플로우가 태그 형식과 main 조상을 직접 검사한다 |

23·24(테스트 블로그 더미 글 · hELLO 스킨 백업)는 착수 단계 항목이라 2026-08-27에 걷었다. 번호는 재사용하지 않는다.

**업로드 파일 최소화** — 배포가 수동이므로 산출물은 이렇게 나와야 한다:

```
붙여넣기: skin.html · style.css · index.xml
업로드  : images/script.js                        ← 매 배포 1개
          images/ph-<slug>-<theme>.v<N>.webp ×30  ← 기본 이미지가 바뀐 배포에서만 (결정 6 개정)
```

폰트는 CDN에서 받는다. 기본 이미지 말고는 `images/`에 두지 않는다 — 빌드가 31개가 아니면 경고한다.

### 카테고리 체계

| # | 결정 | 근거 |
|---|---|---|
| 27 | 카테고리를 **주제 축 + 언어 예외**로 재편했다. 상위 11 / 하위 36 → **상위 14 / 하위 21**, 트리 48줄 → **36줄**. **2026-08-25 티스토리 적용 완료** | 개편 전 `IT`가 122편(45%)을 담은 미분류 통이었고, 같은 레벨에 언어·기술 영역·주제 **세 축이 섞여** 있어 새 글이 갈 곳이 두 군데였다. 1편짜리 하위가 7종, **글이 0편인 하위가 6종**이었다 — 실측에 잡힌 하위는 30종뿐이고 나머지 6종은 빈 껍데기였다 |

> **줄 수는 `분류 전체보기`를 포함해 센다.** 개편 전 1 + 11 + 36 = 48줄, 지금은 1 + 14 + 21 = 36줄이다. `[##_category_##]`가 실제로 그려내는 `<li>` 수다.

**축은 둘이다 — 주제, 그리고 언어.** `Python` `Kotlin·Java` `Go` `PHP`가 언어 축이고 나머지는 전부 주제 축이다. 언어를 남기는 이유는 블로그이기 때문이다 — **언어 이름은 검색과 인지에서 가장 강력한 라벨**이라, `[Python] …` 제목을 단 글을 주제별로 흩으면 검색으로 들어온 독자에게 보여줄 옆 글이 무너진다. 다만 **예외는 예외답게 전부 같은 대우를 한다.** 한 언어만 상위로 올리고 다른 언어를 주제 카테고리 안에 숨기면 그게 축 혼재다.

**상위는 언어 이름만 쓴다.** `Kotlin·Spring`이 아니라 `Kotlin·Java`인 이유다. **프레임워크는 언어 아래로 간다**(`Python/Django·Flask`, `Kotlin·Java` 안의 Spring). 프레임워크를 언어에서 떼어 별도 상위에 두면 "Spring은 웹 프레임워크가 아닌가?"에 답할 수 없다.

**이름 규칙**

| 규칙 | 이유 |
|---|---|
| **`&`를 쓰지 않는다** | URL에서 `%26`, RSS·OG에서 `&amp;`로 이스케이프된다. `·`로 대체 — 쿼리 구분자로 오해될 여지가 없다 |
| **하위에 `/`를 쓰지 않는다** | `상위/하위` 경로 표기와 충돌한다. `CI/CD` → `CI·CD` |
| **하위에서 상위 이름을 반복하지 않는다** | `데이터베이스/DB 이론` → `데이터베이스/이론` |
| **한두 단어로 끝낸다** | 사이드바·글 머리 라벨·브레드크럼에 모두 들어간다 |

**정렬은 편집 판단이지 기계적 규칙이 아니다.** 활성도(최근 24개월 글 수) 순으로 놓으면 `개발 도구` 14편(3)이 `Python` 41편·`데이터베이스` 28편보다 위로 온다 — **이 둘은 최근 24개월 0편이다**(`알고리즘` `Go` `PHP`도 0편). 사이드바만 훑는 방문자에게 블로그의 무게중심을 잘못 보여준다. 반대로 편수 순으로만 놓으면 최근 24개월 16편으로 가장 활발한 `AI`가 9번째로 밀린다. **두 축 어느 쪽도 혼자서는 맞지 않아 손으로 정했다.** 규칙을 만들어 두면 규칙 이름과 실제 순서가 어긋난 채 굳는다. 새 카테고리도 손으로 자리를 잡는다. 현재 순서는 §3에 실측된 그대로다.

**`PHP` 2편은 상위로 유지한다** — 2026-08-25 사용자 판단. 가장 작은 상위(`Laravel` 1 · `Codeigniter` 1)이고 두 글 모두 원문의 90%가 `yum`·`systemctl` 설치 명령이라 `인프라/리눅스`로 옮길 근거도 있었지만, 언어를 예외 없이 같은 레벨에 둔다는 위 원칙과도 맞는다. **"우선" 유지이므로 뒤집힐 수 있다** — 옮기면 상위 13종·기본 이미지 28장(14 slug × 2)이 된다. 이미지는 15 slug 전부 있고 `PHP`도 그중 하나다 — 뒤집히면 `src/assets/placeholders-src/php-*`(임시본이면 `scripts/gen-placeholders.py`의 `php` 항목)과 `DESIGN.md` §6.2의 두 줄만 지우면 된다.

**URL 영향 없음** — 티스토리 글 주소는 `/entry/{제목}` 또는 `/{번호}`라 카테고리를 바꿔도 **글 URL은 변하지 않는다.** 바뀌는 것은 `/category/…` 목록 주소뿐이다.

### SEO

| # | 결정 | 근거 |
|---|---|---|
| 28 | **SEO 레버를 티스토리 소관과 스킨 소관으로 가른다. 티스토리가 넣는 것은 스킨에서 손대지 않는다** | 2026-08-25 라이브 실측(§3). 티스토리가 이미 주입하는 것을 스킨에서 또 넣으면 중복 태그가 되거나 티스토리 것이 이긴다. 반대로 "티스토리가 알아서 하겠지"로 넘긴 것 중 **글 페이지 빵부스러기는 실제로 비어 있다** |

**우리가 건드리지 않는 것** — `canonical` · `og:*` · `twitter:*` · `meta description` · `sitemap.xml` · `robots.txt` · 구글·네이버 사이트 인증. 전부 실측으로 정상 동작을 확인했다.

**우리 레버는 넷이다.**

| 레버 | 스킨이 쥐는 이유 |
|---|---|
| **내부링크** | 관련글·이전/다음·태그·카테고리는 전부 스킨이 치환자를 어디에 놓느냐로 결정된다. **가장 큰 레버다** |
| **헤딩 계층** | `h1`이 몇 개인지는 스킨 마크업이 정한다. 구 스킨은 PC에서 2개를 내보내고 있었다(`V003`) |
| **글 페이지 `BreadcrumbList`** | 티스토리는 카테고리 페이지에만 넣는다. 글에는 없다 |
| **목록 페이지 `<title>`** | `[##_page_title_##]`을 쓰지 않으면 카테고리·태그·검색이 전부 같은 제목이 된다 |

**검증은 두 단계다.** 배포 전 소스 게이트(`/skin-qa-check`의 `SEO001~005`)와 배포 후 프로덕션 실물(`/seo-verify-live`). **이 프로젝트는 배포가 에디터 수동 복붙이라 소스가 맞아도 프로덕션이 틀릴 수 있고, 그 경로는 소스 린트가 원리적으로 잡지 못한다.**

**콘텐츠는 우리 범위가 아니다.** 제목·키워드·본문 품질·이미지 `alt`은 티스토리 에디터에 있고 저장소에 없다. 발견하면 보고만 한다.

### 영역 구조

| # | 결정 | 근거 |
|---|---|---|
| 29 | **`<s_permalink_article_rep>`·`<s_index_article_rep>`는 `<s_article_rep>` 안에 둔다. 목록은 홈까지 `<s_list>` 한 벌로 그린다** | 2026-08-25 첫 배포에서 글 페이지 본문이 통째로 사라졌다. 원인은 이 둘을 최상위에 둔 것이었다 |

**두 영역은 독립 영역이 아니라 `<s_article_rep>`의 하위 영역이다.** 공식 문서가 이 둘을 글 치환자(`<s_article_rep>`) 절 **안에서** 설명하고, "내부에서 사용하는 치환자는 `<s_article_rep>` 내부와 동일하다"고 적은 것이 그 뜻이었다(`docs/tistory-skin-reference.txt:798`). 바깥에 두면 티스토리가 **통째로 버린다 — 에러도, 빈 껍데기도, 미치환 치환자 잔존도 없다.**

배포 실측(2026-08-25, `git-rich-quick.tistory.com`):

| 페이지 | 결과 |
|---|---|
| 글 | `.entry-*` **0종** — 제목·본문·목차·태그·관련글·이전다음이 전부 없음 |
| 홈 | `.card-grid` 자식 0개 — 그런데 `<s_list>`가 대신 그려 줘서 **멀쩡해 보였다** |
| 헤더·사이드바·태그클라우드·페이징 | 정상 |

**홈이 멀쩡해 보인 것이 발견을 늦췄다.** 그래서 린트 `SUB008`을 만들었다 — 짝 검사(`SUB005`)로는 원리적으로 못 잡는다. 짝은 맞고 **위치만** 틀리기 때문이다. 프리뷰 렌더러도 같은 규칙을 몰라 "잘 나온다"고 보여 주고 있었다. 규칙을 모르는 프리뷰는 통과 신호를 위조한다.

**부수 소득이 둘 있다.**

1. **`<s_list>`는 홈에서도 렌더된다.** `[##_list_conform_##]`이 `"전체 글"`로, `[##_list_count_##]`가 전체 글 수로 채워진다. 그래서 홈 목록을 `<s_index_article_rep>`로 따로 그릴 이유가 없어졌고, 카드 마크업이 두 벌에서 한 벌이 되었다.
2. **홈에도 `h1`이 생겼다.** 그 `.list-title`이 홈의 `h1`이다. "홈은 h1이 없는 것이 정상"이라는 예외(`DESIGN.md` §3, `V003`)는 **죽은 영역이 만든 착시**였으므로 지웠다.

| # | 결정 | 근거 |
|---|---|---|
| 31 | **카테고리는 `[##_category_list_##]`(리스트형)를 쓴다. `[##_category_##]`(폴더형)가 아니다** | 2026-08-25 첫 배포에서 폴더형이 나갔다. 둘은 이름만 비슷하고 완전히 다른 UI를 출력한다 |

공식 레퍼런스는 두 줄로만 적어 두었다(`docs/tistory-skin-reference.txt:1719-1720`) — "폴더 형식" / "리스트 형식". 출력 마크업은 어디에도 없다. 그래서 양쪽을 실측했다.

| | `[##_category_##]` 폴더형 | `[##_category_list_##]` 리스트형 |
|---|---|---|
| 구조 | 중첩 `<table>` **19개** | `ul.tt_category` / `li` |
| 링크 | `onclick="location.href=…"` — **`<a href>` 0개** | **`<a href>` 36개** |
| 이미지 | 트리선 GIF **17장** (티스토리 CDN) | 0장 |
| 인라인 `style` | **18개** (`color:#4d4d4d` 9 + `background-color:#ffffff` 9) | **0개** |
| 현재 위치 | `index.xml <tree>`의 `activeColor` | `li class="selected"` |

실측 출처 — 폴더형 `git-rich-quick.tistory.com`(우리 스킨 배포본), 리스트형 `sanggi-jayg.tistory.com`(본 블로그, 2026-08-25).

**폴더형은 조용히 실패하지 않는다. 다른 UI가 나온다.** `tistory.css`의 `.tt_category` 규칙이 한 줄도 매칭되지 않아 티스토리 기본 트리가 그대로 노출되고, `category.js`는 `<ul>`을 못 찾아 물러난다. 배포 화면에 뜬 `├ ▫` 모양의 트리가 그것이었다.

**결과가 셋이다.**

1. **내부링크 36개가 생겼다.** 폴더형에서는 0개였다 — 사이드바 최대 모듈이 크롤러에게 통째로 보이지 않았고, 키보드로도 닿지 않았다. 결정 28이 내부링크를 우리 레버로 꼽아 놓고 정작 가장 큰 링크 뭉치를 `onclick`으로 내보내고 있었다.
2. **미결 14가 사라졌다.** 다크모드에서 트리가 안 읽히는 문제의 원인은 `<tree>`가 인라인으로 박는 색이었는데, 리스트형에는 인라인 `style`이 하나도 없다. `!important` 예외도, `index.xml` 수정(= 스킨 설정 초기화)도 필요 없다. **고친 게 아니라 원인이 없어졌다.**
3. **`index.xml`의 `<tree>`는 이제 아무 데도 쓰이지 않는다.** 지우려면 `index.xml`을 다시 올려야 하고 그러면 설정이 초기화된다(결정 1). 값이 무해하므로 **그대로 둔다.**

   ⚠ **그 블록의 색을 팔레트 대조에 세지 않는다** (2026-08-27). `activeColor 0070f3`은 `--link`의 **옛 값**이고 지금은 `#0064da`다. 화면에 닿지 않으므로 고칠 이유가 없는데, 팔레트를 훑는 사람에게는 «안 고친 리터럴»로 보인다. **이 사실을 `index.xml` 주석으로 적으려다 되돌렸다** — 그 파일은 한 글자만 바꿔도 재업로드 대상이 되고, 재업로드는 스킨 설정을 초기화한다. 주석 한 줄의 값이 그 비용을 넘지 않는다. **`index.xml`에 남길 말은 여기에 적는다.**

린트 `CAT001`이 폴더형을 막는다. 프리뷰 렌더러는 두 형식을 **다른 마크업으로** 그린다 — 이전에는 둘을 같은 리스트형에 매핑해 두어, 폴더형이 나가는 동안에도 프리뷰가 "잘 나온다"고 보여 주었다. 결정 29와 **정확히 같은 실패**가 한 번 더 난 것이다.

### 레이아웃

| # | 결정 | 근거 |
|---|---|---|
| 30 | **사이드바를 통째로 왼쪽에 세운다. 홈·글에서도 켠다** | 카테고리가 이 블로그의 뼈대인데(상위 14 · 하위 21) 홈으로 들어온 방문자가 볼 길이 없었다. 2026-08-25 사용자 요청 |
| 37 | **카테고리 레일의 현재 항목은 알약 배경(`--canvas-soft-2` · `--radius-md`)으로 표시한다.** 왼쪽 2px 막대를 대체한다 | Vercel docs 실측(2026-08-26). 막대를 고른 근거 두 가지가 재보니 성립하지 않았다 — 아래 |

**폭을 통일한 것이 이 결정의 핵심이다.** 레일이 서는 페이지는 `--page-w`가 전부
`--wrap`이다. 페이지마다 폭이 다르면 컨테이너가 다른 폭으로 가운데 정렬되고,
그러면 레일 x좌표가 달라져 **카테고리가 옆으로 뛴다** — 한자리에 두려던 목적이 무너진다.
본문은 `--content-w`(800px)로 잠그고 남는 자리는 비워 둔다.

**실측 (2026-08-27, 로컬 프리뷰 · 스크롤바 15px 포함)**

| 뷰포트 | 레일 x | 본문 x | 거터 | 홈 카드 |
|---:|---|---|---|---|
| 1240 | 24 | 312 | 48 | 3열 **280.3** |
| 1400 | 24 | 292.5 | 28.5 | 3열 340.2 |
| 1440 | 24 | 312.5 | 48.5 | 3열 346.8 |
| 1512 | 24 | 348.5 | 84.5 | 3열 358.8 |
| 1600~ | 56.5~ | 392.5~ | **96** | 3열 **362.7** |

**「레일 x가 고정」이 아니다. 「같은 폭에서 페이지끼리 같다」가 맞다.** 1600px부터는
래퍼(1520)가 뷰포트보다 좁아 가운데 정렬이 살아나므로 레일 x가 56.5 → 216.5(1920)
→ 529(2560)로 움직인다. 이 결정이 실제로 요구한 것은 후자이고 그건 지켜진다 —
1440px에서 홈·글(목차 유/무)·카테고리·보관함 **다섯 페이지 전부** 레일 x=24 ·
본문 x=312.5이고, 글 본문은 목차 유무와 무관하게 정확히 **800.0px**다.

**두 하한.**

- **홈 3열 1240px** — 카드가 **280.3px**로 나온다. `@media`가 보는 1240에서 실사용
  폭은 스크롤바를 뺀 1225다: `(1225 - 48 - 240 - 48 - 카드간격 48) / 3 = 280.3`.
  하한(「280px 아래로 가면 카드 제목이 매번 잘린다」)은 성립하지만 **여유가 0.3px뿐이다.**
  이 값을 근거로 무엇을 더 얹기 전에 다시 잰다.
  1239px 이하는 2열이고 카드가 432px로 오히려 넓다.
- **글 3단 1400px** — `240 + 24 + 800 + 24 + 240 + 여백 48 = 1376`, 스크롤바 15를
  더해 1391이라 1400 안에 들어간다(24는 `--gutter-min`, 실측 28.5). 레일이 300px이면
  1496px이 필요해 **1440 모니터가 3단에서 떨어진다.** 그래서 `--sidebar-w`가 240이다.
- **`--wrap` 1200 → 1400 → 1520** — 마지막 단계는 결정 36이 올렸다. 3단이 거터
  최대치까지 벌어진 상태의 폭이다: `48 + 240 + 96 + 800 + 96 + 240 = 1520`.
  **상한이지 요구 폭이 아니다** — `max-width`라 좁은 화면에서는 거터가 대신 줄고,
  3열 하한은 1240px이 지킨다.

1399px 이하에서 접히는 것은 **목차**다. 카테고리는 모든 데스크톱 폭에서 남는다.
목차는 읽는 중 문서 내 위치를, 카테고리는 사이트 탐색을 맡는다 — 좁아질 때
사이트 탐색을 남기는 쪽이 이 요청의 취지에 맞다.


### 색 · 다크모드

| # | 결정 | 근거 |
|---|---|---|
| 32 | **티스토리 스타일시트가 박아 둔 라이트 전용 색을 `tistory.css`에서 토큰으로 덮는다. 테마 분기는 하지 않는다** | 2026-08-26 배포본 실측에서 다크 본문의 여러 요소가 1.00~3.66:1로 사라졌다 |
| 33 | **다크 캔버스는 순검정이 아니다 (`#0a0a0a`), 본문은 `#b0b0b0`, `--font-smooth`는 테마 토큰** | 미결 5(다크 팔레트 파생값 검증)를 실화면에서 닫은 결과. 사용자 확인 |
| 34 | **`*_group` 치환자는 자기 그룹 래퍼 안에 둔다 — 댓글은 `<s_rp>`, 방명록은 `<s_guest>`** | 2026-08-26 본 블로그 배포에서 `<s_rp>`가 없어 댓글이 통째로 사라졌다. 티스토리가 **빈 문자열로 치환**한다 |
| 35 | **댓글·방명록은 `.comments`/`.guestbook` 짝에 조상 둘을 달아 (0,3,0)으로 덮는다. 자리표시자 한 자리만 body id까지 붙인다** | 2026-08-26 배포 후 다크에서 댓글 본문이 1.16:1로 묻혔다. 원인은 색이 아니라 **특이도** — 댓글 블록이 통째로 지고 있었다 |

**결정 32 — 왜 인라인 보정으로는 안 되는가.**

결정된 대응은 이미 있었다(§5.2 인라인 스타일 오염). 그런데 그 방식은 **글 본문의
`style` 속성**을 `[style*="color: #000000"]`으로 잡는 것이다. 이번 색들은 거기 없다 —
티스토리가 **자기 스타일시트**에서 칠한다. 속성 선택자로는 원리적으로 닿지 않고,
`inline-fix.js`의 휘도 안전망도 인라인 속성만 상대하므로 마찬가지다.

더 나쁜 것은 **특이도**다. `content.css`의 상당수 규칙이 `#tt-body-page`로 시작한다.

```
#tt-body-page blockquote[data-ke-style='style1'] { color:#333 }   (1,1,1)  티스토리
.contents_style blockquote { color: var(--ink-body) }             (0,1,1)  우리 — 진다
```

**라이트에서는 멀쩡했다.** `#333` on `#ffffff` = 12.63:1. 그래서 한쪽 테마에서만
조용히 깨진 채로 배포되어 있었다. 실측 피해:

| 컴포넌트 | 다크 대비 | 실측 |
|---|---|---|
| 오픈그래프 카드 제목·링크 (`#000`) | **1.00:1** | 62곳 / 39편 |
| 인용 `style3`·`box` (배경 `#fcfcfc`) | 다크에 **흰 카드** | 24곳 / — |
| 인용 `style1` + `cite` (`#333`) | **1.66:1** | 7곳 |
| `blockquote p` (`#666`) | 3.66:1 | 전체 57곳 / 25편 |
| 표 `style12` 줄무늬·첫 열 | 1.90~2.27:1 | 9곳 / 6편 |
| 첨부 파일명 (`#333`) | 1.66:1 | 1곳 |

**테마 분기를 하지 않는 것이 이 결정의 핵심이다.** 리터럴을 토큰으로 바꾸기만 하면
다크는 토큰이 따라온다. `@media`·`[data-theme]` 여섯 상태를 다시 재야 하는 구조를
아예 만들지 않는다 — 그 실수를 이 저장소에서 이미 두 번 했다(DESIGN.md §8.1).
덤으로 라이트도 팔레트에 맞춰진다: `style12` 표 머리는 흰 글자를 `#9b9b9b`에 얹어
**라이트에서도 2.78:1**이었고, 같이 고쳐졌다.

**구문 색은 반대 방향이었다.** 티스토리는 코드블록이 있는 글에 `atom-one-light`을
CDN에서 주입하는데 그 `<link>`가 우리 `style.css` **뒤**에 온다. 양쪽 다 `.hljs-keyword`로
특이도가 같아(0,1,0) 순서로 밀렸다 — 우리 팔레트가 **통째로 무효**였고, 라이트 테마 색이
다크 배경에 얹혀 keyword 3.06 · number 3.85 · literal 4.48이었다. 팔레트 전체에
`.hljs ` 접두를 달아 (0,2,0)으로 올렸다.

목록은 `data/tistory-hardcoded-colors.json`이 정본이고 린트 `TIS001`·`TIS002`·`HLJS001`이 지킨다.
**티스토리가 시트를 바꾸면 자동으로 알 방법은 없다** — 목록이 낡으면 린트는 조용히 통과한다.

**결정 33 — 무엇을 바꿨고 무엇을 지켰나.**

| 항목 | 전 | 후 | 이유 |
|---|---|---|---|
| 캔버스 사다리 | `#000000`/`#0a0a0a`/`#111111` | `#0a0a0a`/`#121212`/`#1a1a1a` | 순검정 위 밝은 글자의 번짐. 한 단만 올려 near-black 인상은 남겼다 |
| `--ink-body` | `#a1a1a1` (8.13) | `#b0b0b0` (9.13) | 같은 대비율이라도 light-on-dark는 더 얇고 흐리게 보인다 |
| `--font-smooth` | 전역 `antialiased` | 테마 토큰 (다크 `auto`) | macOS에서 획을 얇게 만들어 다크에서 손해가 크다 |

**지킨 것은 위계다.** 제목과 본문의 명도차가 라이트 2.12 · 다크 1.85다. 본문을 한 단 더
밝히면(`#bdbdbd` → 1.60) 소제목과 `<strong>`이 본문에 묻히기 시작한다. 그래서 멈췄다.

**수치가 대칭인데 왜 체감이 달랐나**가 이 결정의 교훈이다. 바꾸기 전에도 본문 대비는
라이트 8.45 ↔ 다크 8.13으로 사실상 같았다. WCAG 수치만 보면 고칠 것이 없었다 —
그래서 파생값인 채로 배포되었다. **대비율은 하한을 지키는 도구이지 읽히는 느낌의
척도가 아니다.** 다음에도 팔레트는 수치가 아니라 실화면에서 판단한다.

**결정 34 — 댓글이 사라진 자리, 그리고 방명록이 원인을 짚어 준 방식.**

본 블로그 배포 후 사용자가 댓글 permalink를 들고 왔다. 글에는 댓글 2개가 달려 있고
`<s_rp_count>`도 "댓글 2"를 정상 출력하는데, **댓글 UI가 아예 없었다.**

```
서버 HTML:  <div class="comments" id="comments"></div>     ← 비어 있다
```

미치환 치환자도, 빈 껍데기도, 콘솔 에러도 없었다. 티스토리가
`[##_comment_group_##]`을 **인식하고 빈 문자열로 치환한 것**이다. 같은 페이지의 다른
React 앱(`NaverAd`·`Reaction`·`Menubar`)은 전부 정상이었고 `Comment`만 없었다.

**원인은 같은 스킨의 방명록이 짚어 줬다.** 둘은 공식 문서에서 완전히 대칭인데
우리 마크업만 비대칭이었다:

| | 그룹 래퍼 | 결과 |
|---|---|---|
| 방명록 `[##_guestbook_group_##]` | `<s_guest>` 있음 | `<div data-tistory-react-app="Comment">` 정상 |
| 댓글 `[##_comment_group_##]` | **없음** | **빈 문자열** |

**이것은 결정 29와 같은 계열이다** — 치환자 이름은 맞고 짝도 맞고, 틀린 것은
**어느 영역 안에 있느냐**뿐이다. 짝 검사(`SUB005`)로도 `s_article_rep` 중첩
검사(`SUB008`)로도 원리적으로 못 잡는다. `SUB009`를 추가했다.

**프리뷰는 이번에도 통과 신호를 위조하고 있었다** (세 번 겪은 것의 네 번째다).
렌더러가 `comment_group`을 래퍼와 무관하게 치환해 프리뷰에서는 댓글 상자가
멀쩡히 보였다. 이제 래퍼 밖에서는 **빨간 경고 상자**를 그린다 — 티스토리의 침묵을
그대로 흉내 내면 눈으로도 못 찾기 때문이다. 진짜 상자는 `s_rp`·`s_guest` 핸들러가
넣는다.

**교훈은 배치다.** `<s_rp_count>`가 정상 동작한 것이 판단을 늦췄다. "댓글 관련
치환자가 하나는 되는데 하나는 안 된다"는 상태는 래퍼 문제를 가린다. `*_group`
치환자는 앞으로 **자기 래퍼와 한 몸으로** 쓴다.


**결정 35 — 댓글이 보이게 된 자리. 결정 32의 두 번째 얼굴.**

결정 34로 댓글이 *나오게* 된 다음, 사용자가 "다크에서 댓글이 검은색이라 안 보인다"를
가져왔다. 증상은 색이었지만 원인은 색이 아니었다 — **`tistory.css`의 댓글 블록이
한 줄도 적용되지 않고 있었다.**

댓글·방명록은 티스토리가 **React로 런타임에 그린다.** 소스 HTML에는
`<div data-tistory-react-app="Comment"></div>` 빈 껍데기뿐이라 크롤로는 그 시트의
존재조차 보이지 않는다. 결정 32의 `content.css`는 크롤해서 읽을 수 있었지만
이쪽은 원리적으로 못 읽는다. **그래서 특이도를 라이브 DOM에서 직접 쟀다** — 우리
규칙을 head 맨 앞(= 실제 우리 위치)에 주입하고 어디서부터 이기는지 하나씩 봤다.

```
.tt_desc                              (0,1,0)  진다
.comments .tt_desc                    (0,2,0)  진다 ← 상대도 (0,2,0)이고 뒤에 온다
.comments .tt-box-content .tt_desc    (0,3,0)  이긴다
```

우리 규칙은 전부 (0,1,0)이었다. 다크에서 댓글 본문·작성자·**입력 중인 글자**가
`#222`로 나와 캔버스(`#0a0a0a`)에 1.16:1이었다. 쓰는 사람은 자기가 뭘 쳤는지
볼 수 없었다.

**발견이 늦은 이유가 이 결정의 핵심이다.** 같은 블록의 `.tt-box-total` 글자색과
`<input>`은 **이기고 있었다** — 요소 선택자거나 상대에 짝이 없어 안 부딪혔기
때문이다. 절반은 먹고 절반은 지는 상태라 "이 영역이 통째로 무시되고 있다"는
신호가 화면에 나타나지 않았다. **부분적으로 동작하는 것은 전혀 동작하지 않는
것보다 찾기 어렵다.** 결정 34에서 `<s_rp_count>`가 판단을 늦춘 것과 같은 모양이다.

**한 자리만 예외였고, 그것도 조용히 죽어 있었다.** 첫 수정은 전부 (0,3,0)으로
통일했는데, 머지 전 검증에서 빌드한 CSS를 라이브에 주입해 11종을 하나씩 재 보니
**자리표시자만 `#b1b1b1` 그대로**였다. 그 한 자리만 상대가 ID로 시작한다.

```
.comments .tt-inner-g .tt-cmt::before                        (0,3,1)  진다
#tt-body-page .comments .tt-inner-g .tt-cmt::before          (1,3,1)  이긴다
#tt-body-guestbook .guestbook .tt-inner-g .tt-cmt::before    (1,3,1)  이긴다 (방명록)
```

**상대의 특이도를 한 덩어리로 일반화하면 안 된다.** 같은 앱 안에서도 자리마다
다르다. 검증하지 않았으면 "고쳤다"고 기록된 채 안 고쳐진 규칙이 남았을 것이다.

**린트도 같이 고쳤다.** `TIS001`/`TIS002`는 크롤 가능한 `content.css`만 본다.
댓글 시트는 크롤에 안 잡히고 스코프 모양도 다르므로 축을 하나 더 냈다 —
`data/tistory-hardcoded-colors.json`의 `commentRules` 11종과 **`TIS003`**이다.
`.comments`·`.guestbook` **양쪽** 짝을, `idScoped`면 body id까지 대조한다.
한쪽만 있어도 잡는다.

**프리뷰는 여전히 댓글을 못 그린다.** React 런타임이라 원리적으로 재현할 수
없다. 흉내 내는 대신 그 자리에 `[댓글: 티스토리 React가 렌더링]`을 그려
**못 본다는 사실을 표시**한다. 댓글 변경의 최종 확인은 배포 후 라이브뿐이다.
2026-08-26 배포에서 사용자가 확인했다.

**같은 날 프리뷰가 위조한 신호가 하나 더 있었다.** 글 하단 태그에 쉼표가
노출됐는데, `[##_tag_label_rep_##]`가 `<a>` 사이에 `", "` 텍스트 노드를 끼워
넣기 때문이다(`.entry-tags`가 flex라 그것이 익명 flex 아이템이 된다). 프리뷰
렌더러는 태그를 **공백**으로 이어 붙이고 있어 현상이 재현되지 않았다.
구분자를 실물과 맞췄다. **재현하지 않는 검사는 통과가 아니라 침묵이다.**


**결정 36 — 거터를 유동으로 돌린 자리, 그리고 주석이 틀린 채 굳어 있던 산수.**

사용자가 "Vercel docs처럼 좌측 카테고리·우측 목차와 본문 사이에 여백이 있었으면
좋겠다"고 했다. `vercel.com/docs`를 세 폭에서 재 보니 우리와 **숫자가 아니라 모델이**
달랐다.

```
                레일   좌거터   본문   우거터   목차     래퍼
Vercel @1400     250     39      768     48     240     없음(풀블리드)
Vercel @1512     250     95      768    104     240     없음
Vercel @1920     250    299      768    308     240     없음
우리   @1400     240     32      793*    32     240     1400 (가운데 정렬)
우리   @1920     240     32      800     40     240     1400
```

Vercel docs에는 `max-width` 래퍼가 없다. 레일을 뷰포트 왼쪽(24px), 목차를 오른쪽에
못박고 **남는 폭 전부를 두 거터가 균등하게** 먹는다. 우리는 1400px 래퍼를 가운데
정렬하므로 남는 폭이 전부 **바깥 여백**으로 가고 거터는 32px에 고정된다.
사용자가 본 차이는 이것이지 간격 토큰이 아니었다 — **`gap` 값만 올려서는 재현되지 않는다.**

**그런데 값을 올릴 자리 자체가 없었다.** `여백48 + 240 + 32 + 800 + 32 + 240 = 1392`,
래퍼 1400 안에 8px 남는다. 그리고 위 표의 `793*` — 그 8px조차 없었다.
**`@media`와 `100vw`는 스크롤바를 포함한 폭이다.** 1400px 뷰포트의 실사용 폭은 1385라
트랙 합 1392가 7px 넘쳤고, `minmax(0,1fr)`인 본문이 그만큼 눌려 있었다.
`layout.css` 주석은 "1392니까 1400 안에 들어간다"고 **계산까지 적어 둔 채** 틀려 있었다.
800px은 `index.xml <contentWidth>`와의 계약인데 라이브에서 793이었다.

**고른 것은 풀블리드가 아니라 상한 있는 유동 거터다.** 거터가 남는 폭을 먹되
`--gutter-max` 96px에서 멈추고 나머지는 바깥 여백으로 보낸다.

**그래서 `--wrap`을 1400 → 1520으로 올렸다.** 거터가 최대치까지 벌어진 3단의 폭이
`48 + 240 + 96 + 800 + 96 + 240 = 1520`이라, 래퍼를 1400에 두면 거터가 96에
닿기 전에 래퍼가 먼저 상한에 걸려 유동 거터가 절반만 동작한다.
풀블리드로 가면
결정 30(레일 x가 전 페이지 공통) 때문에 홈·목록도 함께 풀어야 하고, 그러면 홈 카드
영역이 1920에서 1,600px가 되어 3열/4열을 다시 정해야 한다. **거터 하나를 위해
홈 그리드 결정을 다시 여는 것은 비용이 맞지 않는다.** 1400~1512 — 노트북 구간 —
에서는 어차피 Vercel과 거의 같은 값이 나온다(28.5·48.5·84.5 대 39·48·95).

**구현의 핵심은 `column-gap`의 백분율이다.** `100vw`로 계산하면 위의 스크롤바 함정을
그대로 반복한다. `column-gap`의 %는 **그리드 컨테이너 자기 content-box** 기준이라
스크롤바가 이미 빠져 있다. 그래서 OS·스크롤바 종류와 무관하게 본문이 정확히 800이
된다(1400·1440·1512·1600·1920·2545 여섯 폭 실측, 전부 800.0).

층이 둘이라 분모가 둘이다. `.layout`은 거터를 둘 품고 `.entry-layout`은 하나만 품는다.
`g = (W-1280)/2` 와 `g = Wc-1040`. 1280·1040은 리터럴이 아니라 `--track-sum-*`로
토큰에서 파생시켰다 — `--content-w`를 바꾸면 둘 다 따라온다.

**거터도 레일처럼 전 페이지 공통이어야 한다.** 결정 30은 레일 x만 말했는데, 레일이
붙박이여도 본문 x가 페이지마다 다르면 홈→글에서 글이 옆으로 뛴다. 그래서 유동 거터를
레일 6종 **전부**에 건다 — 홈·목록에는 목차가 없어 거터가 하나뿐이지만 그 하나를
글 페이지의 왼쪽 거터와 같은 식으로 계산한다(네 폭에서 홈·글·카테고리 x 일치 확인).


**결정 36-b — 색이 아닌 첫 패배. 소제목 굵기.**

같은 작업에서 잡았다. 티스토리 `content.css`에 이 줄이 있다.

```
#tt-body-page h2[data-ke-size] { font-weight: normal }   (1,1,1)
.contents_style h2                                       (0,1,1)  진다
```

결정 32·35와 지는 방식이 똑같다. 다른 점은 **상대가 색이 아니라 굵기를 덮는다**는
것뿐인데, 그 하나가 발견을 늦췄다. 색은 다크에서 "안 보인다"로 나타나 사용자가
신고한다. 굵기는 **두 테마 모두 멀쩡해 보인다.** 우리가 쓴 600이 그냥 반영되지 않을 뿐이라
보고 있어도 "원래 저런가 보다"가 된다.

게다가 상대가 `[data-ke-size]` **있는 것만** 잡는다. 표본 14편 50개 소제목 중 30개(60%)가
그 속성을 갖고 있었고 — 속성은 에디터 버전 탓에 글 단위로 붙는다 — 그 60%만 400,
나머지 40%는 600으로 나왔다(라이브 실측: `/entry/JVM-Garbage-Collection` 12개 전부 400,
`/entry/Redis-Command` 15개 전부 600). **글마다 굵기가 다른 상태가 오히려
"이 규칙이 통째로 무시되고 있다"는 신호를 지웠다** — 결정 35의 "부분적으로 동작하는
것이 전혀 동작하지 않는 것보다 찾기 어렵다"가 세 번째로 반복됐다.

`#tt-body-page .contents_style h2[data-ke-size]`(1,2,1)로 이긴다. **`[data-ke-size]`를
떼면 안 된다** — 속성 선택자는 특이도의 클래스 칸에 들어가므로 떼는 순간 (1,1,1)로
내려가 상대와 같아지고, 같으면 순서가 정하는데 순서는 티스토리가 정한다.

**린트에 축을 하나 더 냈다 — `TIS004`.** `TIS001`/`TIS002`가 보는 `rules`와 상대는
같은 시트지만, 저쪽은 "다크에서 안 보인다"로 드러나고 이쪽은 아무 신호도 없다.
`data/tistory-hardcoded-colors.json`의 `typographyRules` 3종을 `#tt-body-page
.contents_style ` + marker와 문자 그대로 대조하고, **속성 선택자만 뗀 모양은 따로
잡는다**(있는데 지는 상태가 아예 없는 것보다 나쁘다). 두 실패를 모두 반대로 시험해
잡히는 것을 확인했다.

**프리뷰는 이번엔 위조하지 않았다.** 결정 35에서 티스토리 `content.css`를 프리뷰가
싣도록 고쳐 둔 덕에, 이전 빌드를 프리뷰에 넣으면 `h2[data-ke-size]`가 그대로 400으로
나온다. 고친 검사가 다음 버그를 실제로 잡은 첫 사례다.


**결정 37 — 막대를 알약으로. 근거를 뒤집은 게 아니라 재본 것이다.**

Vercel docs 내비는 현재 항목에 알약 배경(`rgb(31,31,31)` · `radius 6px`)을
쓴다. 우리는 왼쪽 2px 막대였고, `DESIGN.md §5.3`에 **배경을 쓰지 않는 이유**가 적혀
있었다 — "240px 레일에서 블록 면적이 크고 §4의 촘촘한 세로선을 끊는다".

**그 두 문장은 재보니 둘 다 틀렸다.**

| 적혀 있던 근거 | 실제 |
|---|---|
| "블록 면적이 크다" | 알약은 `li`가 아니라 `a`에 걸린다. 세로 폭이 막대와 **같다** — 프리뷰에서 선택/비선택 네 줄 전부 34.4px |
| "세로선을 끊는다" | 하위 목록의 세로선은 `ul`의 `padding-left`(`--sp-3` 12px) 자리에 있고, 알약이 왼쪽으로 나가는 거리는 `--sp-2`(8px)다. **실측 간격 5px, 선은 그대로 지나간다** |

색은 새 토큰을 만들지 않았다. Vercel의 `#1f1f1f`은 우리 사다리에 없지만
`--canvas-soft-2`(다크 `#1a1a1a` / 라이트 `#f5f5f5`)가 그 자리다. **한 곳에 쓰자고
사다리에 칸을 더하지 않는다.** 대비는 이미 §2에 적혀 있던 값 그대로다 —
`--link`가 `--canvas-soft-2` 위에서 다크 5.49 / 라이트 5.02, 둘 다 AA.

좌우 여백은 같은 크기의 음수 마진으로 상쇄한다. **글자가 움직이면 안 된다** —
프리뷰에서 네 줄의 글자 왼쪽 끝이 전부 552.5px, 글 수 배지 오른쪽 끝이 전부
779.5px로 같았다. 알약이 레일 밖으로 8px씩 흘러나가지만 `.layout`의 `--pad-x`가
20~24px이라 어느 폭에서도 잘리지 않는다(넓은 폭은 프리뷰 실측, 좁은 폭은 토큰 산수).


**결정 38 — 소제목 앵커. 그리고 `#`을 텍스트로 넣지 않은 이유.**

목차 링크가 소제목을 가리키는 유일한 통로였는데 **목차는 소제목이
3개 이상일 때만 생긴다**(실측 68%). 나머지 32%의 글에서는 소제목 하나를 짚어
공유할 방법이 없었다. 그래서 앵커는 목차와 조건을 공유하지 않는다 — 소제목이
하나여도 붙는다.

**`#`은 CSS `::before`가 그린다. 텍스트 노드로 넣지 않는다.** 소제목의
`textContent`는 두 곳에서 다시 쓰인다 — 목차 링크의 라벨(`toc.js`)과 검색결과에
실리는 제목이다. 텍스트로 넣으면 목차에 `개요#`이 뜨고 색인에도 그대로 들어간다.
`::before`는 복사·선택에도 딸려오지 않는다.

**id 만드는 자리를 목차와 합쳤다** — `util.headingsWithIds()`. 둘이 각자 소제목을
세면 필터가 한 글자만 달라도(예: 빈 소제목을 한쪽만 거른다) 그 뒤 번호가 전부
한 칸씩 밀린다. 그런데 목차는 그대로 뜨고 앵커도 그대로 보인다 — **눌러 보기
전에는 아무 신호가 없다.** 결정 32·35·36-b와 같은 종류의 실패다.

**`:focus-visible`가 아니라 `:focus`를 쓴다.** 기본이 `opacity: 0`이라, 포커스
상태에서 안 보이면 **포커스 링까지 같이 사라진다.** `:focus-visible`의 휴리스틱은
브라우저가 정하고 프로그램적 포커스에는 붙지 않는다. 여기서 빗나가면 키보드
사용자는 자기가 어디 있는지 모른다 — 보이는 쪽으로 틀린다. 호버가 없는
입력장치(`@media (hover: none)`)에서는 나타날 계기가 없어 항상 보여 준다.

프리뷰에서 확인한 것: 목차 없는 글(`page.html`, 소제목 2개)에도 앵커가 붙고,
목차 있는 글에서 목차 링크 4개의 `href`가 소제목 `id` 4개와 **하나도 어긋나지
않으며**, 목차 라벨에 `#`이 섞이지 않았다. 마우스 클릭·키보드 Enter 둘 다 해시가
그 소제목을 가리켰고, 포커스 링이 화면에 보였다.


**결정 39 — 글 제목은 `display-lg` 32px을 유지한다. `display-xl` 48px로 올리지 않는다.**

Vercel docs의 `h1`은 56px/600 `ls -0.06em`이고 우리는 32px/700
`-0.022em`이다. 숫자만 보면 두 단 차이지만, 그 숫자를 가져오려면 `DESIGN.md §3`
두 줄을 고쳐야 했다 — **한글에 음수 자간을 아낀다**(최대 -0.028em)와 스케일 상한
48px이다. 2026-08-26 사용자 판단: **지금 크기가 적절하다.**

**이건 "나중에 다시 볼 것"이 아니라 닫힌 항목이다.** 결정 36-b·37·38은 우리가
재지 않은 것을 재서 고쳤지만, 여기서 Vercel과 다른 것은 실수가 아니라
**다른 언어를 조판하고 있기 때문**이다.
`display-xl`(48px)은 계속 예비로 남는다.


**결정 40 — JS가 만드는 클래스는 `docs/hooks.md §5.6`이 정본이고, 린트가 그 표를 읽는다.**

`TODO js-class-css-drift`. `BND004`는 **반대 방향만** 본다 — JS가 *찾는* 클래스가
`skin.html`에 있는가. JS가 *만드는* 클래스(`.code-copy` `.table-scroll`
`.heading-anchor` …)는 마크업에 자리가 없어 그 검사에 애초에 안 걸린다. 그래서
JS에서 클래스를 새로 만들거나 이름을 바꾸고 CSS를 안 고쳐도 **아무 검사도 안
켜졌다.** 스타일 없는 날것 DOM이 뜨고 에러는 없다. PR #29 리뷰에서 물었을 때
드러났는데, 재보니 그 PR이 낸 구멍이 아니라 **원래 비어 있던 축**이었다.

**목록을 린트에 복사하지 않았다.** §5.6 표를 파싱해서 읽는다. 복사해 두면 문서와
갈라지고, 갈라진 뒤에는 **코드 쪽이 조용히 이긴다** — 문서에 클래스를 더해도
검사는 모른 척한다. 표를 못 읽으면(절 이름이 바뀌거나 표 모양이 달라지면)
통과가 아니라 **오류**를 낸다. 검사가 꺼지는 것과 통과하는 것은 다른 일이고,
이 저장소는 그 둘을 헷갈려서 세 번 데었다.

**축을 둘로 나눈 이유.** `BND006`(등재 → CSS, 오류) 하나로는 부족하다. JS에서만
이름을 바꾸면 문서와 CSS는 죽은 이름을 그대로 붙들고 있고, `BND006`은 **그 죽은
규칙을 보고 통과한다** — 위조된 통과 신호의 네 번째 판이다. `BND007`(등재 → JS,
경고)이 그 자리를 막는다. 경고인 것은 문서가 구현보다 먼저 오는 경우가 정상이기
때문이다(§5.6의 존재 이유가 "skin-style이 미리 스타일을 써둘 수 있게"다).

**예외에는 이유를 함께 적어야 한다.** `.external-link`는 CSS 규칙이 없는 것이
정상이다 — 표시는 `.external-icon`이 맡고, 이쪽은 JS가 같은 링크를 두 번 손대지
않으려고 붙이는 표식이다. 이런 항목을 그냥 경고로 내면 **상시 경고**가 되고,
상시 경고는 린트를 통째로 무시하게 만든다(`TOK002` 오탐으로 이미 한 번 데었다).
그래서 §5.6에 「CSS 규칙이 없는 것이 정상인 클래스」 목록을 두되, **이름만 적은
줄은 예외로 치지 않는다.** 이유가 없으면 "정상"과 "아직 안 한 일"을 구분할 수
없고, 구분 못 하는 예외는 상시 경고와 같은 값이다.

**CSS는 `src`만 본다.** `dist`는 `src`의 사본이라, `src`에서 규칙을 지워도 낡은
`dist`에 남아 있으면 통과해 버린다(`TIS00x`가 `src`만 보는 것과 같은 이유).
선택자만 세는 것도 같은 종류의 방어다 — 주석에 이름을 적어 두거나
`content: ".code-copy"` 같은 선언 값에 스친 것을 "규칙이 있다"로 세면 안 된다.

**그래서 검사 자신을 검사한다** — `npm run test:lint`. 저장소 사본을 12가지로
일부러 망가뜨리고(CSS 규칙 삭제, 접두만 남김, CSS 주석에만 남김, 선언 값에만 남김,
JS만 개명, JS 주석에만 남김, 문서에만 등재, §5.6 절 삭제, 예외에서 이유 삭제,
예외 제목 삭제, 예외 두 개) 그 코드가 뜨는지 본다. 규칙을 썼다는 것과 그 규칙이
조건을 재현한다는 것은 다르다. 이 저장소가 세 번 데인 지점이 정확히 그 차이였다.

**그 12개는 장식이 아니었다.** 처음 쓴 파서에 결함이 둘 있었고 둘 다 이 테스트가
잡았다 — (1) 예외 항목이 줄바꿈으로 이어지면 그 다음 예외를 조용히 잃었고,
(2) JS 주석에 남은 옛 이름을 "살아 있는 사용처"로 셌다. 둘 다 린트는 초록불이었다.


**결정 41 — 사이드바는 카테고리·최근 글 2개만 쓴다. 나머지 5개 모듈 마크업은 남겨 둔다.**

`skin.html`에는 `<s_sidebar_element>`가 7개 있다(카테고리·공지·최근 글·인기 글·
최근 댓글·태그·방문자). 라이브에는 **카테고리와 최근 글 둘만 나온다.**
2026-08-26 사용자가 티스토리 관리 → 꾸미기 → 사이드바에서 그렇게 설정했다.
**결함이 아니라 선택이다.** 결정 2(모바일웹 자동 연결 OFF)와 같은 종류 —
스킨 코드가 아니라 블로그 설정이 정하는 값이다.

**마크업은 지우지 않는다.** 티스토리는 블로그에 등록된 슬롯 수만큼만 렌더하므로,
슬롯을 늘리면 나머지 5개가 다시 나온다. 지워 두면 그때 다시 써야 한다.

**이 결정을 남기는 이유는 같은 오판을 막기 위해서다.** 2026-08-26
`/seo-verify-live`가 이것을 **배포 회귀로 잡았다.** 근거가 강해 보였다 —
`skin.html`은 첫 배포(12:56) 이후 무변경인데 모든 페이지 타입에서 내부링크가 줄었고,
목록 4종(보관함·태그·방명록·검색0건)이 **똑같이 13 → 5**로 떨어졌다. 페이지마다
실린 글이 다른데 감소폭이 같으면 공유 컴포넌트, 즉 사이드바다. 거기까지는 맞았다.
**틀린 것은 원인 판정이다** — 사용자 설정 변경을 배포 사고로 읽었다.

**교훈: "무엇이 바뀌었나"와 "누가 바꿨나"는 다른 질문이다.** 실측이 가리키는
컴포넌트가 맞아도, 그것이 코드 사고인지 설정 변경인지는 실측으로 안 갈린다.
스킨 코드로 못 고치는 자리(결정 2·15·41)를 만나면 **사용자에게 먼저 묻는다.**

`data/seo-baseline.json`을 이 상태로 다시 찍었다. 옛 기준선을 두면 `--compare`가
매번 오류 1·경고 7을 내고, **상시 경고는 검사를 통째로 무시하게 만든다**
(`TOK002` 오탐 전례). 회귀 감시를 살려 두려면 기준선이 현재 의도를 담아야 한다.

**결정 42 — 치환자 하나만 담은 요소의 라벨·구분자는 `:empty` 가드를 함께 단다.
린트 `BND008`이 지킨다.**

2026-08-26 라이브를 열어 보니 홈 카드에 `댓글`이, 사이드바 최근 글에 `·`가
**값 없이** 떠 있었다. 원인은 치환자가 조용히 빈 문자열을 낸 것이다.

| 자리 | 치환자가 낸 것 | 화면 | 실측 |
|---|---|---|---|
| 홈 카드 `.post-rp` | 빈 문자열 | `댓글` | **13 / 13** |
| 사이드바 `.side-rp` | **빈 자식 span** `<span id="commentCountOnRecentEntries292"></span>` | `·` | **5 / 5** |
| 글 페이지 `.entry-rp` | 숫자 | `댓글 0` | 정상 |

**세 자리가 같은 치환자 가족인데 하나만 값을 낸다.** 왜 목록 쪽이 비는지는
**재지 못했다** — 댓글이 0이라 그런지, 목록 영역에서 원래 안 채워지는지 안 갈랐다.
그러나 **고치는 방법은 그 답과 무관하다**: 값이 없으면 장식도 그리지 않는다.

**`::before`를 끄는 것으로는 부족하다.** `.post-meta`·`.side-meta`·`.list-head`가
전부 `flex` + `gap`이라 빈 칸의 gap이 유령으로 남는다. 요소째 감춘다.
사이드바는 `:empty`도 안 통한다 — 티스토리가 **빈 자식 span**을 넣어 `:empty`가
거짓이 된다. `:has(> span:empty)`를 쓴다. `:has`는 살아 있는 선택자라 티스토리
스크립트가 나중에 숫자를 채우면 구분자가 스스로 돌아오고, 미지원 브라우저에서는
지금 화면과 같다 — 나빠지지 않는다.

**이게 두 번째다. 그래서 린트를 냈다.** 결정 35의 `tag-comma`가 똑같은 모양이었다
(태그 값이 비고 `,`만 남았다). 그때는 이 자리를 고치고 프리뷰 픽스처만 손봤다.
같은 실패가 다른 자리에서 다시 나왔으므로 이번에는 **규칙으로 만든다** —
`BND008`은 "치환자 하나만 담은 요소에 `::before`/`::after` 장식이 있으면
`:empty`(또는 `:has()`) 가드를 요구한다".

**린트가 켜지자마자 셋째를 찾았다** — `.list-count::before { content: "글 " }`에
가드가 없었다. 라이브 홈에서는 `글 275`로 값이 나오지만 모든 목록 영역이
그렇다는 보장이 없다. 같이 고쳤다. **규칙으로 만들지 않았으면 이건 못 찾았다.**

**린트의 오탐도 같이 막았다.** 첫 판이 `.entry-tags a::before`(태그 앞의 `#`)를
잡았는데, 거기 장식은 **자손** `<a>`에 붙어 있어 치환자가 비면 `<a>`째 사라진다 —
잡을 대상이 아니었다. 그래서 `BND008`은 장식이 **그 요소 자신**에 붙은 경우만 본다.
`test-empty-decor.py`에 "떠야 한다" 6케이스와 **"뜨면 안 된다" 3케이스**를 같이 뒀다
(결정 40과 같은 이유 — 새 린트가 실제로 켜지는지는 린트 자신이 증명하지 못한다).

**가드 자체가 조용히 죽는 자리도 같이 막는다.** `:empty`는 치환자가 여는 태그에
**붙어 있을 때만** 참이 된다 — 제 줄로 내리면 값이 사라져도 공백 텍스트 노드가
남아 영영 거짓이다. 이 함정은 `layout.css:9`에 이미 적혀 있었지만 **검사는
없었다.** 가드가 있는지만 보면 "가드는 있는데 죽어 있는" 상태가 초록불로 통과한다.
그래서 `BND008`은 치환자 앞뒤 공백까지 본다. `:has()` 가드는 자식 노드를 보므로
공백에 걸리지 않는다 — 사이드바가 이미 그 형태다.

**프리뷰가 이 조건을 한 번도 그리지 않았다는 것이 더 큰 문제였다.**
`render.py`가 댓글 수를 세 영역 모두 `(_i * 7) % 13`으로 채우고 있었다 —
**라이브에서 비는 자리를 프리뷰는 항상 숫자로 그렸다.** 그래서 결함이 배포에서만
보였다. 이제 라이브를 그대로 재현한다(목록은 빈 문자열, 최근·인기 글은 빈 자식 span,
글 페이지는 숫자). 넷 중 하나는 값을 채워 **두 상태가 한 화면에** 있게 했다 —
전부 비우면 반대 방향(값이 있을 때 라벨이 제대로 나오는가)을 못 본다.
이것이 CLAUDE.md가 말하는 **네 번째 위조된 통과 신호**였다.

**결정 43 — 언어 라벨은 「누가 썼는가」로 가른다. 글쓴이가 쓴 것만 믿는다.**

결정 18은 "`data-ke-language`를 무시하고 자동 감지를 쓴다"였다. **그 판단은 그대로
유효하다.** 다만 근거가 바뀌었다 — 그때는 "라벨이 틀리더라"였는데, 이제는
**누가 그 라벨을 만들었는지**를 안다.

| 신호 | 누가 만들었나 | 실측 | 믿는가 |
|---|---|---|---|
| `<code class="language-X">` | **글쓴이** (마크다운 펜스) | 프로브에서 6종 전부 정확 | ✅ |
| `data-ke-language` | 에디터 | 285개(39%) 중 `javascript` 44개가 전부 오답 | ❌ (결정 18) |
| `<pre>`의 클래스 | 에디터 자동 감지 | 41블록 최빈값이 **`reasonml` 6개** | ❌ |

`<pre>` 클래스 분포가 결정적이었다(2026-08-26, 홈 12편 41블록):
`reasonml` 6 · `dts` 4 · `less` 3 · `routeros` 2 · `angelscript` 2 · `maxima`·
`ceylon`·`jboss-cli`·`1c`·`isbl`·`arduino`·`stylus`·`awk` 각 1.
**이 블로그에 ReasonML도 RouterOS도 Maxima도 없다.** 티스토리 에디터가 highlight.js
전체 언어팩으로 자동 감지해 박은 노이즈이고, `javascript` 44개가 오답이던
이유이기도 하다. **우리가 후보를 10종으로 좁힌 것과 같은 문제다.**

**두 신호는 마크업으로 완전히 갈린다.** 홈 12편 41블록에서 `<code class="language-*">`와
`data-ke-language`/`<pre class>`가 **한 블록도 겹치지 않았다.** 그래서 과거 글과
새 글을 가르는 규칙을 따로 만들 필요가 없다 — **마크업이 이미 갈라 준다.**

**글쓴이가 쓴 언어에는 임계·한글 가드를 적용하지 않는다.** 그 가드들은 *우리가
추측할 때* 틀리지 않으려는 장치이지, 글쓴이가 직접 쓴 것을 되묻는 장치가 아니다.

**번들에 없는 이름은 라벨만 달고 칠하지 않는다.** `typescript`라고 쓴 것을 우리가
`java`로 칠하는 쪽이 더 나쁘다. **언어인지 모르는 이름은 라벨도 달지 않는다** —
펜스에는 언어가 아닌 것도 들어온다(`info`·`warning` 같은 표식, 그리고 오타).
그런 이름에 "Info" 라벨을 달면 **틀린 정보를 화면에 새로 만드는 것**이다.

**받아들인 대가: 이 신호는 편집 왕복 한 번에 사라진다.** 실측 —
`language-python` → `pre.isbl`, `language-kotlin`·`language-javascript` → **둘 다
`pre.angelscript`**. 사라지면 자동 감지로 **조용히** 되돌아가고 화면에 신호가 없다.
그래도 이 설계를 고른 이유는 **되돌아간 상태가 지금과 같아서 나빠지지 않기
때문이다.** 대안(언어도 텍스트 표식으로 받기)은 왕복에 강하지만 본문에 표식이
드러난다 — 코드블록에서는 그 대가가 더 크다.

**검사** — `scripts/probe-code-detect.mjs`가 `authorLanguage()`를 **직접 불러**
17개 표본으로 고정한다(`npm run check`의 `test:code`). 그중 셋이
`reasonml`·`angelscript`·`isbl`이다: **에디터 라벨을 다시 믿게 되는 순간 빨간불이
켜진다.** 프리뷰 본문 픽스처에도 펜스형 블록 3종(`python`·`typescript`·`info`)을
넣어 화면으로 갈리는지 볼 수 있게 했다 — 없으면 또 통과 신호가 위조된다(결정 42).

**결정 44 — 코드 구문 색은 전용 토큰 6종으로 분리한다.**

`DESIGN.md §8`이 "구문 하이라이팅 색 규정이 없다"를 미결로 달고 있었다.
2026-08-26 라이브에서 `getComputedStyle`로 재 보니 **대가가 두 가지**였다.

| `.hljs-*` | 전 (라이트/다크) | 문제 |
|---|---|---|
| (코드 기본색) | `#171717` / `#ededed` | 기준 |
| 키워드·타입·내장 | `--link` `#0064da` / `#3291ff` | **유일한 유채색** |
| 주석 | `--ink-mute` `#707070` / `#8f8f8f` | |
| **문자열·숫자** | `--ink-body` `#4d4d4d` / `#b0b0b0` | **기본색보다 흐리다 — 강조가 아니라 후퇴** |
| **함수명** | `--ink` `#171717` / `#ededed` | **기본색과 완전히 같다 — 구분 0** |

**색이 넷이 아니라 사실상 셋이었다.** 같은 글에서 색이 붙은 문자는 43.3%
(8,188자 중 3,545자, span 246개)였다 — **칠하는 양이 부족한 게 아니라 칠한 색이
서로 안 갈렸다.** 하이라이팅에서 가장 먼저 눈에 들어와야 할 문자열·숫자가
기본 코드색보다 흐리게 칠해지고 있었고, 함수명은 `<span>`을 붙이고도 화면에
차이가 없었다. **두 테마 모두 같은 구조였다.**

**두 번째 대가가 더 비쌌다.** 범용 토큰을 빌려 쓰면 코드블록이 그 토큰에 묶인다.
2026-08-26에 실제로 청구됐다 — `--link`를 "링크니까" 기준으로 잡았다가
코드블록 배경 위에서 AA 미달이 드러났다(§8.1). **이 결정의 진짜 값어치는
색이 예뻐지는 것이 아니라 그 연결이 끊기는 것이다.**

**여섯 면 전부에서 쟀다.** 코드블록은 `--canvas-soft` 위에만 놓이지만, 이
저장소는 한 면만 보고 정했다가 두 번 데었다(`--ink-mute`, `--link`).

| 토큰 | 라이트 (canvas/soft/soft-2) | 다크 (canvas/soft/soft-2) |
|---|---|---|
| `--code-keyword` | `#0064da` 5.48 / 5.25 / 5.02 | `#3291ff` 6.25 / 5.91 / 5.49 |
| `--code-string` | `#0b7038` 6.20 / 5.94 / 5.68 | `#5ec27a` 8.93 / 8.45 / 7.85 |
| `--code-number` | `#8a5300` 6.33 / 6.06 / 5.81 | `#e0a458` 9.07 / 8.58 / 7.97 |
| `--code-fn` | `#6f42c1` 6.51 / 6.24 / 5.97 | `#c4a7f7` 9.63 / 9.12 / 8.47 |
| `--code-comment` | `#707070` 4.95 / 4.74 / 4.54 | `#8f8f8f` 6.12 / 5.79 / 5.38 |
| `--code-deleted` | `#d60000` 5.44 / 5.21 / 4.99 | `#ff6166` 6.74 / 6.38 / 5.93 |

**최저 4.54.** 열여덟 칸 전부 AA를 넘는다.

**keyword와 comment는 `--link`·`--ink-mute`와 지금 값이 같다. 우연이 아니라
의도다.** 화면을 바꾸지 않고 **연결만 끊는 것**이 이 토큰의 목적이고, 그래야
이 변경의 시각적 차이가 **문자열·숫자·함수명 셋으로 좁혀진다.** 무엇이 바뀌었는지
눈으로 확인할 수 있는 크기로 유지하는 것이 이 저장소의 방식이다(결정 33).
앞으로 둘은 각자 움직인다.

**속성·변수는 일부러 무채색으로 남겼다** (`--ink-body`). 메타·태그도 마찬가지다
(`--ink-mute`). **한 줄에 유채색이 넷을 넘으면 강조가 강조를 잡아먹는다** —
여기까지가 이 팔레트의 상한이다. 중립이 중립을 따라가는 것은 의도이고,
토큰을 더 만들면 "색을 정하지 않았다"는 사실이 오히려 흐려진다.

**부수 효과 — `--error`는 코드블록에서 손을 뗐다.** 유일하게 CSS 파일에 적혀
있던 사용처 `.hljs-deletion`이 `--code-deleted`로 갔다. `tokens.css`의 두 주석
(`--link`·`--error`가 "코드블록 안에서도 쓰인다")도 전제가 깨졌으므로 같이
고쳤다 — **결정 42에서 배운 그대로다.**

**`--error`·`--link`는 코드블록 밖에 사용처가 남아 있다.** `scripts/build.mjs`의
`ACCENT` 맵이 본문 인라인색 `#ee2323`(10곳)·`#006dd7`(12곳)을 다크에서 두 토큰으로
매핑하고, 그 결과는 `dist/style.css`에만 나간다 — `src`를 grep하면 주석밖에 안 나온다.
값을 바꾸면 놓인 면(코드블록 `--canvas-soft`가 아니라 본문 `--canvas`)에서 다시 잰다.
린트 `TOK006`이 빌드 산출물까지 훑어 토큰 참조를 센다.

**검사** — 규칙을 주석으로만 두면 이 저장소에서 「규칙은 있는데 검사가 없는」
상태가 된다. 린트 `HLJS002`가 구문 색 규칙의 `color: var(--X)`를 훑어
`--code-*`가 아니면 오류를 낸다(무채색 예외 둘만 허용). 저장소 사본에서
`--code-keyword`를 `--link`로 되돌려 **실제로 뜨는 것**을 확인했고, 주석 안의
`--link`는 잡지 않는 것도 확인했다. **대비값은 이 린트가 모른다** — 결정 44의
표가 정본이고 손으로 잰 것이다.

⚠ **`.hljs-deletion`과 `.hljs-addition`은 지금 도달하지 않는다.** `diff`가 번들에
없다(`code.js`의 `LANGS`). 그래도 코드 토큰을 쓴 것은, 언젠가 diff를 넣었을 때
`--error`를 통해 코드블록이 다시 묶이는 것을 막기 위해서다.

**결정 45 — 본문 첫 소제목이 글 제목과 같은 것은 글쓰기 습관이다. 스킨은 건드리지 않는다.**

2026-08-26 홈 카드 12편의 HTML을 받아 페이지 `<h1>`과 본문 첫 소제목을 글자
단위로 대조했다. **소제목이 있는 10편 전부에서 첫 `h2`가 `<h1>`과 완전히 같았다**
(나머지 2편은 본문에 소제목이 아예 없는 잡담 글이다).

스킨의 세 자리에서 증상으로 나온다 — 글 페이지에서 제목이 두 번 보이고, 목차 첫
항목이 제목이며(`util.headingsWithIds()`가 `h2`·`h3`를 그대로 긁는다), 홈 카드
발췌가 자기 제목으로 시작한다(13개 중 11개).

**2026-08-27 사용자 확인 — 글쓰기 습관이고, "이러면 SEO에 유리하겠지"라는 막연한
기대로 해 온 것이다.** 그래서 이 항목을 닫는다. **다시 열지 않는다.**

**근거 셋.**

1. **결정 28이 이미 선을 그었다.** "콘텐츠는 우리 범위가 아니다 — 제목·키워드·
   본문 품질·이미지 `alt`은 티스토리 에디터에 있고 저장소에 없다. 발견하면
   보고만 한다." 이건 그 정의에 정확히 들어맞는다.
2. **스킨에서 감추는 선택지는 동기 자체를 배신한다.** JS로 첫 소제목을 감춰도
   **검색엔진에는 계속 보인다** — 원시 HTML은 그대로다. 색인상의 중복은 안 없어진다.
   SEO를 기대해 만든 것을 SEO로는 풀 수 없는 자리다.
3. **글쓴이가 의도한 소제목을 스킨이 지우는 것**이 되고, 제목을 나중에 고치면
   조용히 되살아난다. 화면에 신호가 없는 동작을 새로 만드는 셈이다.

**결정 48 — 목차 접이식 문턱은 3단 경계(1400px)와 같다. 목차 유무는 첫 페인트 전에
인라인으로 판정한다. 터치·키보드 경로를 막지 않는다 (2026-08-27).**

2026-08-27 셀프 리뷰(CSS·JS 리뷰 + 라이브 실측)에서 나온 여섯을 한 번에 고쳤다.
공통점은 **1440px 데스크톱 + 마우스 말고는 한 번도 안 봤다**는 것이다.

| 자리 | 전 | 실측 | 후 |
|---|---|---|---|
| 목차 접이식 경계 | `components.css`·`toc.js` 1024/1025 — **레일이 서는 경계**를 그대로 빌려 썼다 | 라이브 1280px: 목차가 본문 **위**에 382px 블록으로 펼쳐진 채 `pointer-events:none`, 12항목이 첫 화면을 밀어냈다. `layout.css`의 2단은 1400부터라 그 사이 375px 구간이 문서(§4 표 "1025~1399 접이식")·주석(`layout.css`의 「글 페이지 : 본문 + 목차」 ↔ 「1400px~ : 글 페이지 3단」)·코드 셋 사이에서 갈려 있었다 | 1399/1400. 목차가 옆 칸에 설 때만 "라벨화"가 성립한다 |
| `body.no-toc` | `toc.js`만 붙였다 — `script.js`가 도착해야 | 32%의 글이 첫 페인트 뒤 한 번 움직였고(hooks.md §5.1이 인정), `script.js`가 **안 오는** 경로(404·광고차단·CDN 실패)에서는 모든 글이 빈 목차 트랙을 안고 2단으로 남았다. `@media (scripting: none)`은 「JS 꺼짐」만 받는다 | `skin.html`의 `.entry-body` 직후 인라인 스크립트가 첫 페인트 전에 판정. `toc.js`의 `markNoToc()`는 폴백. **둘의 조건(h2·h3, 빈 것 제외, 3개 미만)은 같아야 한다.** `script.js` 없이도 목차 없는 글은 1칸이 된다. 소제목 3개 이상인 글은 스크립트 없이는 목차도 못 만드니 빈 트랙이 남는다 — 그건 전과 같다 |
| 코드 복사 버튼 | `:hover`/`:focus-within`에서만 opacity 1 | 터치엔 hover가 없고 `<pre>`는 포커스 불가 — **영영 안 보였다.** `.heading-anchor`는 이미 `@media (hover: none)`으로 풀어 둔 것을 한쪽만 고친 상태였다 | `hover: none`에서 항상 표시, 같은 자리의 언어 라벨은 접는다 |
| 검색 입력 | 14px | iOS Safari는 16px 미만 입력에 포커스하면 페이지를 확대한다 | `hover: none`에서 16px |
| 라이트박스 | 클릭 리스너뿐 | `<img>`는 포커스 대상이 아니라 키보드·스크린리더에는 진입로가 없었다. `cursor: zoom-in`은 광고만 했다. 닫기(Esc·트랩·포커스 복귀)는 잘 돼 있어 대비가 컸다 | 대상 `figure img`에 `tabindex=0`·`role=button`·`aria-label`, Enter/Space로 연다. figure당 하나라 탭 정거장은 실측 최대 19개 |
| `.side-rp` 가드 | `.side-rp:empty, .side-rp:has(…)` 한 목록 | 선택자 목록은 forgiving이 아니다 — `:has` 미지원 브라우저에서 규칙 **전체**가 버려져 `:empty`까지 죽는다. 주석 "미지원 브라우저에서는 지금 화면과 같다"가 틀려 있었다 | 두 규칙으로 분리 |

**교훈 둘.** (1) 브레이크포인트는 **그 자리에서 무엇이 바뀌는 경계**에 건다 — 레일 경계를
목차에 빌려 쓴 것이 원인이고, 결정 36의 "스크롤바 산수"와 같은 종류의 미끄러짐이다.
(2) 「JS 꺼짐」과 「JS가 안 옴」은 다른 경로다. `scripting: none`이 후자도 받는다고 믿고 있었다.

**검사.** 이 변경이 새로 만든 「두 곳에 같이 적혀야 하는 상수」 둘 — `no-toc` 인라인 ↔
`toc.js`의 임계·선택자, 접이식 경계 1399 ↔ 1400(components·layout·toc.js) — 은 린트
`BND010`이 대조한다. 어느 한쪽이라도 못 찾으면 통과가 아니라 오류다. 임시 사본에서
`MIN_HEADINGS`만 4로, `components.css`만 1024로 바꿔 둘 다 빨간불이 켜지는 것을 확인했다.
`hover: none`·키보드 진입로는 린트가 원리적으로 못 본다 — 경계면이 아니라 **입력 장치**의
문제다. 프리뷰도 마우스 데스크톱만 그린다. QA 뷰포트 목록에 1280을 더했고(그 목록에
없어서 못 봤다), 터치는 배포 후 실기기에서 본다.

**프리뷰로 확인한 것(2026-08-27).** `page_toc` 1280px — 목차가 51px 토글로 접히고
`aria-expanded="false"`, 본문 y=871(라이브 1280 실측은 목차 블록 382px이 본문 위에 있었다).
1440px — 800/240 2칸, 속성 없음·`pointer-events:none`. `page` — 인라인이 `no-toc`를 붙여
1칸이고, `script.js`를 뺀 사본에서도 같다. 라이트박스 — `figure img`에 Enter → 열림·닫기
버튼 포커스·`body.is-lightbox-open`, Esc → 닫힘·포커스가 이미지로 복귀.
**못 본 것:** `hover: none` 분기(복사 버튼 상시 표시·검색 16px)는 터치 에뮬레이션이 없어
소스로만 확인했다 — 배포 후 실기기에서 본다.

**결정 49 — 파비콘·블로그 아이콘은 `--link` 파랑으로 통일하고 `npm run icons`가 만든다. 관리 화면에 올리며 스킨은 손대지 않는다 (2026-08-27).**

사용자가 가져온 원본은 인디고(`#6366f1`) 바탕에 흰 `>_` — 150×150 엔트리 하나짜리 비압축
ICO 93KB였다. 두 가지를 정했다.

- **색은 스킨의 `--link`(`#0064da`)다.** 인디고는 스킨에 없는 색이라 브랜드 색이 둘이 된다.
  `DESIGN.md` §7의 "두 번째 유채색 금지"는 스킨 CSS 얘기라 규범 위반은 아니지만, 탭·댓글
  아이콘·화면이 한 색이어야 한다는 것이 사용자 선택이다(비교본을 보고 골랐다).
- **파일은 셋이고 용도가 다르다.** `favicon.ico`(관리 → 파비콘, 16·32·48 엔트리 PNG 압축
  2KB)와 `icon-48.png`(관리 → 아이콘 — 티스토리 정의는 「댓글·방명록에서 닉네임 앞에 보여
  주는 아이콘, 16~48px」)가 실제로 올라가고, `apple-touch-icon.png`는 자리가 없어 예비다.

**스킨은 건드리지 않는다.** 파비콘은 티스토리가 `<link rel="icon">`으로 주입하고(라이브 head에
지금은 0개 — 관리 화면에 안 올라간 상태), 블로그 아이콘은 React 댓글 UI가 그린다. `images/`에는
기본 이미지 말고 두지 않는다(결정 6). 그래서 이 결정은 배포 절차(USAGE)만 바꾸고 `dist/`는 안 바꾼다.

**원본은 ICO가 아니라 같은 픽셀의 PNG(`favicon-src-150.png`, 1.8KB)로 둔다.** `sharp`가 ICO를
못 읽고, ICO는 원본 포맷이 아니라 배포 포맷이다. 재색칠은 단순 치환이 아니라 픽셀마다
「얼마나 흰가」를 재서 파랑↔흰색 사이로 다시 섞는다 — 치환이면 글리프 가장자리에 인디고 테가 남는다.

**결정 50 — 1024px 이하에서는 헤더 안 칩 한 줄이 상위 카테고리를 낸다. 하위는 칩에 없다. 헤더는 640px 이하에서 압축한다 (2026-08-27).**

2026-08-27 셀프 리뷰 5번. 390px 라이브 실측 — 헤더 159px(뷰포트의 19%), 카테고리 레일은
`.content` **뒤**라 홈에서 5,800px, 글에서 15,300px 아래. `[##_blog_menu_##]`도 비어 있어
검색 말고 갈 곳이 없었다. 결정 30이 "카테고리가 이 블로그의 뼈대라 홈에 들어온 방문자가
봐야 한다"고 레일을 세웠는데, 결정 2로 `/m/`을 끈 뒤 **유일한 모바일 경로**에는 그 길이 없었다.

**세 안을 프리뷰 위에 목업으로 그려 사용자가 골랐다** (390px, 라이트·다크).

| 안 | 첫 화면 | 왜 아닌가 / 왜인가 |
|---|---|---|
| ① 헤더 아래 접이식 「카테고리 ▾」 | 한 줄 버튼 | 눌러야 보인다. 최근 글 모듈이 같이 올라오면 250px을 더 먹어 모바일에서 빼야 했다 |
| ② 햄버거 드로어 | ☰ 하나 | 눌러야 보이고, 포커스 트랩·스크롤 잠금·복제 DOM — 이 규모에 가장 무겁다 |
| **③ 상위 카테고리 칩 한 줄** | `전체 · 인프라 42 · Python 41 · …` | **열지 않아도 보인다** — 결정 30의 취지를 첫 화면에서 충족한다. 상태 없는 정적 링크라 조용히 실패할 자리가 없다 |

**구현.** `skin.html` 헤더 안에 빈 `<nav class="cat-chips">`를 두고 `cat-chips.js`가 사이드바
트리에서 상위 14종 + 「전체」를 읽어 채운다. 파서는 `category.js`의 함수를 **그대로 가져다
쓴다** — 둘이 각자 파싱하면 레일과 칩이 다른 목록을 낼 수 있고 화면에는 신호가 없다(결정 38과
같은 부류). 현재 가지는 티스토리의 `selected`와 URL 대조로 켜고(`.is-current` + `aria-current`),
하위 카테고리 페이지에서는 그 상위 칩이 켜진다. 못 채우면 빈 채로 남고 CSS가 `:empty`로
감춘다 — 폴더형 트리·JS 실패 전부 오늘 화면(칩 없음)으로 물러난다. 1025px~에서는 레일이
있으므로 CSS가 감추고, DOM은 폭과 무관하게 항상 만든다.

**받아들인 대가.** 하위 21종은 칩에 없다 — 카테고리 페이지에 들어가면 그 목록이 하위 역할을
한다. "뒤에 더 있다"는 힌트는 마지막 칩이 오른쪽에서 잘려 보이는 것뿐이다. 페이드·그림자는
두지 않았다(DESIGN §7).

**헤더 압축.** `.site-nav:empty { display: none }` — 라이브에서 메뉴가 비어 있는데 767px 이하
규칙의 padding·border가 빈 줄로 13px을 먹고 있었다. 640px 이하에서 브랜드 설명을 접고 검색을
96px로 줄여 브랜드·검색·토글이 한 줄에 든다(159 → 73px, 칩 줄까지 129px — 프리뷰 실측). 메뉴를 나중에 설정하면
`.site-nav`는 전처럼 아래 줄로 돌아온다 — 감추지 않는다.

**검사.** `.cat-chips`는 `BND009`(마크업 ↔ CSS), `.cat-chip`·`.cat-chip-count`·`.is-all`·
`.is-current`는 `docs/hooks.md` §5.6·§8 등재로 `BND006`·`BND007`이 본다. 칩의 **내용**(레일과
같은 14종인가)은 검사가 없다 — 같은 파서를 쓰는 것이 그 대신이다.

**프리뷰로 확인한 것(2026-08-27, iframe 실측).** 390px 홈 — 칩 15개(「전체」 + 14), 줄 높이 55px,
칩 30px, 가로 넘침 없음, 브랜드 설명 숨김, 검색 96px. 390px `category`(하위 `Python/성능과
동시성`) — 상위 **Python** 칩이 `.is-current` + `aria-current`, 칩 줄이 그 칩 쪽으로 12px
스크롤. 768px — 칩 줄 보임, 브랜드 설명은 보임. 1440px — 칩 줄 `display: none`, 레일 그대로.
라이브 조건(메뉴 비어 있음)은 프리뷰 픽스처가 메뉴를 그리므로 `.site-nav`를 비워서 재현했다 —
`:empty`로 사라지고 헤더 본체 73px. **못 본 것:** 실기기 가로 스크롤 관성, 라이브의 실제
`selected` 위치(카테고리 페이지 배포 후 확인).

**결정 51 — 빌드가 CSS 주석을 벗기고 산출물 용량 예산을 지킨다. 코드 하이라이트는 유휴 시간에 청크로 돌리고 20,000자 넘는 블록은 건너뛴다 (2026-08-27).**

2026-08-27 셀프 리뷰 성능 항목. 실측 — `dist/style.css` 158,962B 중 **주석이 76,843B(50%)**. 방문자가
매번 받는 렌더 차단 CSS의 절반이 한국어 해설이었고, 편집기 붙여넣기도 두 배였다. "minify하지
않는다"(결정 14·`skin-build`)의 근거는 `[style*="color: #000000"]` 인라인 보정 선택자의 **공백**인데,
주석 제거는 공백·선택자를 건드리지 않는다 — minify가 아니다. 근거·사고 서술은 `src/styles/*.css`에
그대로 있고, 라이브 CSS에는 파일 경계 마커(`/* ── tokens.css ── */`) 한 줄씩만 남는다. 문자열과
`url(…)` 안은 건너뛴다(data: SVG에 `/*`가 올 수 있다). 결과 **76.4KB(-52%)**, gzip 40.6 → **12.8KB**(실측).

**예산.** `style.css` ≤ 96KB, `images/script.js` ≤ 80KB — 넘으면 빌드가 실패한다. 조용히 커지는 것을
막는 것이 목적이고, 넘으면 무엇을 뺄지 정한 뒤 예산을 고친다(`scripts/build.mjs`).

**하이라이트.** `highlightAuto`는 후보 10개 문법을 전부 돌리므로 비용이 길이 × 10이고, 블록 20개면
200회 파싱이 `DOMContentLoaded`에서 **동기로** 돌았다 — 그 뒤에야 목차·진행바가 초기화됐다. 이제
`requestIdleCallback`(없으면 `setTimeout 0`) 청크로 프레임 예산 안에서 돌고 부트는 곧바로 다음
모듈로 넘어간다. **20,000자를 넘는 블록**(로그 덤프)은 감지·하이라이트·라벨을 건너뛰고 복사 버튼과
줄번호 규칙만 붙는다. 화면 차이: 코드 색이 첫 페인트 뒤 수십 ms 늦게 온다 — 전에도 스크립트 뒤였다.

**하지 않은 것 — `hljs` 분리 로드 (미결 `hljs-split`).** 번들 67KB 중 **72%(46KB)**가 highlight.js이고
`<pre>`가 없는 홈·목록·방명록에서도 매번 파싱된다. 진짜 해법은 `images/hljs.js`로 쪼개 `<pre>`가
있을 때만 싣는 것인데 **업로드 파일이 하나 는다**(결정 6). 결정 46이 "1개" 제약을 이미 풀었으므로
막힌 것은 아니지만, 배포 절차·`TOK007`류 검사·hooks 계약이 같이 바뀌어야 해서 따로 연다. 지금은
평가만 남긴다: 모바일 중급 기기에서 46KB 파싱·평가는 10~20ms 급이다.

---

## 2. 플랫폼 제약 (사실)

공식 문서: https://tistory.github.io/document-tistory-skin/ (24페이지 전체 통독)

### 파일 구조

```
SKIN ─┬─ index.xml
      ├─ skin.html
      ├─ style.css
      ├─ preview.gif (112x84) / preview256.jpg / preview560.jpg / preview1600.jpg
      └─ images/     ← image, script, css 등 나머지 전부
```

**파일업로드 탭의 목적지는 파일명으로 갈린다** (2026-08-25 실측 — 공식 문서에 없다).

| 올린 파일 | 실제 위치 |
|---|---|
| `script.js` 등 일반 파일 | `…/skin/images/` |
| `preview.gif` · `preview256.jpg` · `preview560.jpg` · `preview1600.jpg` | `…/skin/` **루트** |

그래서 미리보기 이미지도 편집기에서 올릴 수 있다. **zip 업로드는 받지 않는다** — 스킨
보관함의 "직접 업로드"로 스킨 패키지를 넣는 경로는 동작하지 않았다.

**`style.css`는 스킨 루트의 링크 파일로 서빙된다** — 라이브 HTML이
`https://tistory1.daumcdn.net/tistory/3356137/skin/style.css?_version_=…`를 `<link>`로 건다
(2026-08-27 실측). 그래서 CSS 안의 `url(./images/x.webp)`는 `…/skin/images/x.webp`, 즉
`script.js`가 있는 바로 그 자리로 풀린다. 티스토리가 붙이는 `?_version_`은 자기 링크에만
붙고 우리 CSS의 상대경로에는 안 붙는다 — 기본 이미지 파일명에 버전을 박는 이유(결정 6).

### 치환자 구조

- **값 치환자** `[##_이름_##]` → 값으로 치환
- **그룹 치환자** `<s_이름>…</s_이름>` → 반복 / 조건 / 영역 선언 세 역할
- 브라우저는 이 문법을 모른다 → **로컬에서 그냥 열면 깨진다**

### 페이지 타입 (`[##_body_id_##]`)

`tt-body-index` 홈 · `tt-body-page` 글 · `tt-body-category` · `tt-body-archive` 보관함 · `tt-body-tag` · `tt-body-search` · `tt-body-guestbook` · `tt-body-location`

### 반드시 알아야 할 제약

| 제약 | 영향 |
|---|---|
| **`index.xml`을 바꾸면 스킨의 모든 설정이 초기화된다** | 변수 추가·수정 시 사용자 설정이 날아간다 |
| **`[##_category_##]`는 카테고리 트리를 통째로 렌더링한다** | 마크업 수정 불가. 고정 클래스 `tt_category` `link_tit` `category_list` `link_item` `sub_category_list` `c_cnt`에 CSS만 가능. 접기/펼치기는 JS로 DOM 조작 |
| **`[##_comment_group_##]`은 React 앱이 렌더링한다** | `tt-*` 클래스에 CSS 오버라이드만 가능. 대신 기능 추가가 자동 반영된다 |
| **모바일 전용 스킨 슬롯이 없다** | 반응형이 유일한 대응 수단 |
| **`<s_t3>`는 필수** | 빠뜨리면 댓글·공유 등 티스토리 공통 JS가 전부 죽는다 |
| **`[##_list_rep_title_##]`에는 New 아이콘 `<img>`가 섞여 나온다** | 순수 텍스트는 `[##_list_rep_title_text_##]` |
| **`<a [##_prev_page_##]>`** | 링크가 아니라 `href`를 포함한 **속성 문자열째** 삽입된다 |
| **홈은 최신순 자동 반복이라 글을 선별할 수 없다** | 커버를 쓰지 않는 한 "대표이미지 있는 글만 홈에" 같은 건 불가능 |
| **본문 래퍼는 `<div class="tt_article_useless_p_margin contents_style">`** | `class="contents_style"` 정확일치로 찾으면 오래된 글이 누락된다 |

### 쓸 수 있는 것

| 치환자 | 용도 |
|---|---|
| `<s_article_related>` | 같은 카테고리의 다른 글. `text_type` / `thumb_type`을 알려준다 |
| `<s_article_prev>` `<s_article_next>` | 이전/다음 글 |
| `<s_list_image>` `[##_list_image_##]` | 카테고리 대표이미지 (목록 상단 배너에 사용) |
| `<s_list_empty>` | 검색 결과 0건 화면 |
| `<liststyle>` (index.xml) | **카테고리 관리 화면에서 카테고리별 목록 스타일 선택.** `[##_list_style_##]`가 class로 출력 |
| `<s_page_rep>` | '페이지' 기능. 없으면 글 치환자로 대체된다 |
| `[##_revenue_list_upper_##]` / `_lower_##]` | 홈·목록 상·하단 광고 자리 |
| `entriesOnPage` / `entriesOnList` (index.xml) | 홈·목록의 글 개수 |
| `<variables>` (index.xml) | 스킨 설정 화면. `STRING / SELECT / IMAGE / BOOL / COLOR` |

---

## 3. 실측 데이터 (2026-08-24 기준)

전수 275편. 본문 내부 구성 중 일부는 최신 50편 표본이며 그렇게 표기했다.

### 대표이미지

| 범위 | 보유율 |
|---|---|
| 최신 20편 (**홈 노출 범위**) | **85.0%** |
| 최신 40편 | 87.5% |
| **전체 275편** | **45.1%** (124/275) |

연도별: 2026년 85% · 2025년 87% · 2024년 69% · 2023년 55% · 2022년 29% · 2021년 32% · 2019년 35%
→ **최근에 챙기기 시작했다. 홈은 최신순이라 85% 구간만 본다.**

### 제목 길이

| 범위 | 중앙값 | 40자 초과 |
|---|---|---|
| 전체 275편 | 26자 | 17% |
| 최신 50편 | 42자 | 52% |
| **최신 20편 (홈 노출분)** | **49자** | **75%** |

### 카테고리 (2026-08-25 개편 적용 후 재실측)

**상위 14종 / 하위 21종 · 트리 36줄** (개편 전 상위 11 / 하위 36 · 48줄). 결정 27을 본다.
아래 표의 순서가 **블로그 사이드바의 실제 노출 순서**다 — 티스토리 관리 화면에서 드래그로 정한 값을 그대로 옮겼다.

| # | 상위 (편수) | 하위 (편수) |
|---:|---|---|
| 1 | **인프라** (42) | 쿠버네티스 2 · 모니터링 13 · 데이터 파이프라인 11 · CI·CD 5 · 리눅스 11 |
| 2 | **Kotlin·Java** (15) | — |
| 3 | **Python** (41) | 기초 8 · 성능과 동시성 15 · Django·Flask 10 · 라이브러리 8 |
| 4 | **PHP** (2) | Laravel 1 · Codeigniter 1 |
| 5 | **아키텍처** (25) | 분산 시스템 13 · 원칙 12 |
| 6 | **데이터베이스** (28) | MySQL 23 · Redis 3 · 검색엔진 2 |
| 7 | **네트워크** (12) | — |
| 8 | **보안** (13) | — |
| 9 | **AI** (19) | — |
| 10 | **코드 품질** (31) | Clean Code 16 · 리팩토링 9 · 테스트 6 |
| 11 | **Go** (11) | — |
| 12 | **알고리즘** (15) | — |
| 13 | **개발 도구** (14) | Git 9 · 에디터·터미널 5 |
| 14 | **기록** (7) | — |

- **상위 직속 글은 하위가 없는 7종**(Kotlin·Java · 네트워크 · 보안 · AI · Go · 알고리즘 · 기록)**에만 있다.** 하위를 가진 나머지 7종은 직속이 0편이라 상위 편수 = 하위 합계다.
- **접두사 충돌 없음** — 14종 중 어느 이름도 다른 이름의 접두사가 아니다. `[data-cat^="…"]` 선택자가 성립한다 (DESIGN.md §6.2).
- **이름에 `&`가 없다.** 개편 전 `Kotlin & Java`·`Search Engine`이 만들던 `%26`·`&amp;` 문제가 사라졌다. 다만 `코드 품질`·`개발 도구`·`데이터 파이프라인`처럼 **공백은 남아 있어** 선택자는 여전히 따옴표로 감싼다.

**기본 이미지가 쓰일 글**(= 대표이미지 없는 글)이 많은 곳:
`데이터베이스/MySQL 15/23` · **`코드 품질/Clean Code 15/16`** · `Python/성능과 동시성 10/15` · `알고리즘 9/15` · `아키텍처/분산 시스템 9/13` · `아키텍처/원칙 9/12` · `Go 9/11` · `인프라/리눅스 9/11` · **`코드 품질/리팩토링 9/9`**

상위 기준으로는 `코드 품질 25/31` · `인프라 24/42` · `Python 21/41` · `아키텍처 18/25` · `데이터베이스 17/28` 순이고, 반대로 `Kotlin·Java`는 1/15뿐이다.

> **크롤링이 닿지 않는 것** — 관리 화면의 `비공개용 (3)`은 비공개 글이라 실측에 잡히지 않는다. 위 275편에 포함되지 않았다.

### 코드블록

| 지표 | 값 |
|---|---|
| `<pre>` 총계 | **728개** |
| `data-ke-language` 지정 | 285개 (**39%**) — 그나마 라벨이 틀림 |
| 라벨 분포 | python 149 · **javascript 44 (실제 JS 0개)** · java 24 · bash 21 · kotlin 17 · shell 11 · sql 9 · c++ 4 · html 4 · go 1 |
| **한국어가 섞인 블록** | **239개 (33%)**, 한글 총 16,933자 |
| 가장 긴 줄 | 중앙값 58자 · 90퍼센타일 112자 · **최대 1,777자** |
| 80자 초과 줄을 가진 블록 | 191개 (26%) |

### 인라인 스타일 오염

| 항목 | 값 |
|---|---|
| 인라인 `color` 보유 글 | 59편 (21%) · **609곳** · 17종 |
| 다크에서 죽는 색 | **14종 593곳** — `#000000` 221 · `#333333` 165 · `#252525` 107 · `#666666` 21 · `#555544` 20 · `#383a42` 16 · `#006dd7` 12 · `#404040` 11 · `#ee2323` 10 · `#353638` 6 · `#ad0000` `#555555` `#242424` `#001d35` 각 1 |
| 라이트에서 죽는 색 | 3종 16곳 — `#eeffff` 13 · `#8a8a8a` 2 · `#f8f8f2` 1 |
| `background-color` | **11종 106곳** — `#ffffff` 48 · `#f8f8f8` 14 · `#212121` 13 · `#f2f2f2` 9 · `#fcfcfc` 8 · `#dddddd` 6 · `#f9f9f9` 3 · `#f1f1f1` 2 · `#fbfaf8` `#272822` `#f6f7f8` 각 1 |
| 인라인 `font-family` | **13종 275곳** — `Script` 106 · `AppleSDGothicNeo-Regular` 75 · `Noto Sans DemiLight` 55 · `Tahoma` 10 · `-apple-system` 6 · `fixedsys` 6 · `Noto Serif KR` 5 · `dotum` 4 · `Menlo/Consolas/Monaco/monospace` 2 · `monaco` 2 · `Ubuntu Condensed` 2 · `hack` 1 |

→ **색 17종 + 배경 11종 = 28개 규칙으로 전수 커버된다.**

### 본문 구성 (최신 50편 표본)

이미지 78%·최대 19장 · 인라인 `<code>` 274개·글당 최대 68개 · 소제목 3개 이상 68%·최대 25개 · 본문 중앙값 2,813자·최대 30,314자 · 표 14%·최대 4열 · 외부링크 44%·65개 · iframe 2%

### 검색엔진에 보이는 것 (2026-08-25 라이브 실측)

> 구 스킨(hELLO) 시절 실측이다. 현재 라이브 수치는 `data/seo-baseline.json`(`/seo-verify-live`)이 정본이다.

**티스토리가 자동으로 넣는 것** — `canonical`(PC는 자기 자신, `/m/`은 PC를 가리킴) · `og:*` · `twitter:*` · `meta description` · `article:published_time`/`modified_time` · 구글·네이버 사이트 인증. `robots.txt`는 `/search` `/guestbook` `/manage`를 막는다. `sitemap.xml`은 **625 URL**(글 550 = 275편 × PC/모바일, 카테고리 71, 홈 1, 태그 1, 방명록 2).

**JSON-LD는 페이지 타입마다 다르다.**

| 페이지 | 티스토리가 주입하는 타입 |
|---|---|
| 홈 · 보관함 · 태그 · 검색 · 방명록 | `WebSite` |
| 글 | `BlogPosting` — **빵부스러기 없음** |
| 카테고리 | `BreadcrumbList` |

**콘텐츠 쪽 관측** — 표본 글의 본문 이미지 17장 중 `alt`가 있는 것은 3장. 에디터에서 쓰는 것이라 **스킨으로 고칠 수 없다.**

**글 주소가 두 형태이고 둘 다 산다** — `/entry/{제목}`과 `/{번호}`가 모두 HTTP 200을 낸다. `canonical`은 **항상 `/entry/{제목}`**을 가리키므로 중복 색인 문제는 없다. 현재 치환자가 내보내는 링크는 전부 문자 형태다(홈 30 · 카테고리 30 · 글 15). 다만 관리 → 블로그 → **주소 설정을 "숫자"로 바꾸면 내부링크가 전부 비정규 주소를 향하게 된다.**

**목록 2페이지 이후는 색인에서 홈으로 접힌다** — `/category/Python?page=2`의 `canonical`이 `/category/Python`이 아니라 **사이트 루트**다. `/?page=2`도 루트를 가리킨다. 티스토리 동작이라 스킨으로 못 고친다. → **깊은 글로 가는 경로를 페이징에만 의존하면 안 된다.** 카테고리 트리·태그·관련글이 그만큼 더 중요해진다.

**대표이미지 없는 글의 `og:image`는 티스토리 기본 이미지다** — `t1.daumcdn.net/tistory_admin/static/image…`. 결정 5·7의 **기본 이미지는 CSS 배경이라 `og:image`를 바꾸지 못한다.** 즉 기본 이미지 30장은 **사이트 안에서 보이는 그림일 뿐, 검색·공유 썸네일은 그대로 티스토리 기본이다.** AI 이미지로 바꿔도 같다 — 이 작업의 효과를 검색 CTR로 기대하면 안 된다.

**`[##_page_title_##]`이 페이지 타입별로 내보내는 실제 값**

| 페이지 | 출력 |
|---|---|
| 홈 | `상쾌한기분` — **블로그 제목 그 자체** |
| 카테고리 | `'Python' 카테고리의 글 목록` |
| 태그 목록 | `태그 목록` |
| 보관함 | `'2026/08 글 목록` (여는 따옴표만 있다 — 티스토리 쪽 결함) |
| 검색 | `'리팩토링'의 검색결과` |
| 방명록 | `방명록` |

→ 구 스킨은 `[##_page_title_##] — [##_title_##]` 형태라 **홈 제목이 `상쾌한기분 — 상쾌한기분`이었다.** 린트 `SEO003`이 막는다.

---

## 4. 미결 / 착수 전 확인할 것

> **새 항목은 번호 대신 슬러그를 쓴다** (`tree-color`, `notice-scope` 처럼).
> 여러 세션이 동시에 "다음 번호"를 집으면 반드시 겹친다 — 2026-08-25에 세 번 연속으로
> 겹쳤고(9→11, 11→13, 13→14) 매번 병합 때 손으로 옮겨야 했다.
> **기존 번호는 그대로 둔다.** `미결 12`처럼 다른 문서가 번호로 참조하고 있다. 닫힌 번호는 아래 표로 옮긴다.

| # / 키 | 항목 |
|---|---|
| 9 | **비공개 3편 배치** — 관리 화면의 `비공개용 (3)`. 크롤링에 잡히지 않아 §3의 275편에 없다. 공개로 돌릴지, 새 트리 어디에 넣을지 미정 |
| 10 | **`알고리즘`이라는 이름** — 15편 중 13편이 백준·프로그래머스 풀이라 실질은 `코딩테스트`다. 이름을 바꾸면 통계 1편(정규화와 표준화)이 갈 곳을 잃어 그대로 뒀다 |
| 11 | **카테고리 목록의 줄이 들쭉날쭉해진다** — 결정 7이 대표이미지 **없는 카드만** 썸네일을 감추므로, 이미지가 섞인 카테고리(`데이터베이스/MySQL` 23편 중 8편만 보유)에서는 어떤 줄에만 썸네일이 있다. 기본 이미지로 풀 문제가 아니라 목록 레이아웃에서 정할 문제다. 선택지는 셋 — ① 카테고리 목록에서 썸네일을 전부 감춘다 ② 자리를 비워 둔 채 정렬을 유지한다 ③ 결정 7을 뒤집어 기본 이미지를 그냥 쓴다. **구현은 ①의 변형이다** — `components.css`의 `#tt-body-category .post:not(:has(.thumb-img)) .thumb { display: none }`이 기본 이미지만 감추고 진짜 대표이미지는 남긴다(`docs/hooks.md` 카테고리 목록 절). 그래서 원 문제는 그대로이고 ①·②·③ 중 최종 선택은 미정 |
| 12 | **카테고리 목록 상단 배너를 무엇으로 채울까** — 결정 7의 배너는 `<s_list_image>`가 관리 화면에 설정돼 있어야 나오고 없으면 블록째 사라진다. **기본 이미지를 배너로 대신 쓸지**, 아니면 카테고리마다 대표이미지를 직접 등록할지 정해야 한다 |
| `hljs-split` | **highlight.js를 `images/hljs.js`로 분리해 `<pre>`가 있을 때만 싣을 것인가** — 번들 67KB의 72%. 업로드 파일 +1(결정 6·46), 배포 절차·검사·hooks 계약이 같이 바뀐다. 결정 51이 평가만 남겼다 |
| 13 | **GSC(구글 서치콘솔) 연결 여부** — 사이트 인증은 이미 되어 있으나 MCP는 붙어 있지 않다. 붙이면 "어떤 글을 개선·통합할지"를 실측으로 판단할 수 있지만, 그건 **스킨 루프가 아니라 콘텐츠 루프**다 (대상이 275편의 글이지 스킨이 아니다). OAuth 설정이 필요하므로 사용자 결정 사항 |

**닫힌 번호** — 바깥 문서가 번호로 참조하므로 행을 지우는 대신 여기로 옮긴다.

| 번호 | 무엇 | 어디로 갔나 |
|---|---|---|
| 1 | 스킨 편집기의 파일 개수·용량 제한 | **실용 범위는 실측으로 확인**(2026-08-27) — `images/` 31개(script.js + WebP 30, 약 0.2MB) + 미리보기 4장(약 0.3MB)을 파일업로드 탭이 거부 없이 받았다. 상한 자체는 여전히 공식 문서에 없지만, 이 스킨이 올릴 파일은 이 수를 넘지 않으므로 닫는다. 넘게 되면 `scripts/build.mjs`의 `PLACEHOLDER_BASE`를 jsDelivr로 |
| 4 | 모바일웹 OFF의 광고 수익 영향 | 본 블로그는 2026-08-26 배포 시점에 이미 OFF였다(`0f8ca65`). "전환 전에 잰다"는 전제가 사라져 닫는다 |
| 5 | 다크 팔레트 파생값 검증 | 결정 33 |
| 6 | 기본 이미지 도안 | SVG 15장으로 닫았다가(2026-08-27 오전) 같은 날 결정 5·6 개정으로 **WebP 30장**(`src/assets/placeholders/`)이 됐다. 빌드가 `dist/images/`로 복사하고 `--ph-*`를 3블록으로 내며, 린트 `BND003`·`TOK007`이 `npm run check`에서 돈다. 삽화는 결 3(결정 46)으로 확정해 같은 날 배포했다 |
| 7 | 스킨 미리보기 이미지 4종 | `src/preview/` · `scripts/gen-preview.mjs`. 파일업로드 탭으로 올리면 스킨 루트로 간다(§2 파일 구조) |
| 14 | `index.xml` `<tree>` 색의 다크모드 주입 | 결정 31 — 리스트형 치환자에는 인라인 `style`이 없어 원인이 사라졌다 |

---

## 5. 진행 중 바로잡은 것

조사 과정에서 잘못 말했다가 실측으로 정정한 항목이다. 같은 실수를 반복하지 않기 위해 남긴다.

| 처음 말한 것 | 실제 |
|---|---|
| "기존 글 문단 **대부분에** 인라인 폰트가 박혀 있다" | 글 하나만 보고 일반화한 것. 실제 인라인 `font-family`는 16%(8/50). 다크모드에 더 걸리는 건 인라인 `color` |
| "대표이미지 **80%**" | RSS가 주는 최신 50편 표본이었다. 전수는 **45.1%** (다만 홈 노출분은 85%) |
| "제목 중앙값 **42자**" | 범위마다 다르다. 전체 26자 / 최신 50편 42자 / **홈 노출분 49자** |
| "로컬 미리보기는 치환자 때문에 어렵다 — mock 렌더러가 필요하다" | 티스토리에 프리뷰 API가 있어 실제 데이터로 렌더된 HTML을 받을 수 있다. **단 이번 프로젝트는 API를 쓰지 않기로 해 결국 mock 렌더러로 간다** |
| "카테고리별 기본 이미지면 충분하다" | 카테고리 목록 페이지에서 같은 그림이 최대 15번 반복된다. `#tt-body-category` 분기가 필요하다 |
| **인라인 보정 선택자를 `[style*="color:#000000"]`로 씀** | 실제 마크업은 `style="color: #000000;"` — 콜론 뒤 공백. **609곳 중 608곳이 공백형**이라 이 선택자는 1곳에만 걸린다. 다크모드 보정 28개 규칙이 사실상 전부 무효였다. 지금은 빌드가 `data/inline-styles.json`에서 두 형태를 모두 생성한다 |
| "홈에는 대표이미지가 있는 글만 올리면 된다" | 커버를 쓰지 않는 홈은 **최신순 자동 반복**이라 선별이 불가능하다 |
| "확산모델이 만드는 래스터는 가장 줄여도 1MB라 이 자리에 들어갈 수 없다" (결정 6 구판) | 800×500 WebP q75는 장당 수십 KB다. 못 들어간 자리는 `data:` 인라인이었지 `images/`가 아니었다 — 공식 문서가 `images/`에 image 업로드를 명시한다. "업로드 파일 1개" 제약은 플랫폼이 아니라 우리 선택이었고, 2026-08-27 AI 이미지로 가기로 하면서 풀었다 |
